# 交接文档 — 2026-09-02

> 写给下一个接手 `benzoin-dg-restored` 的会话。**当前有两个 Claude 窗口在同一个
> checkout / 同一个分支上并行工作,还有一个无关的 NHC 项目在同一 SLURM 账号上跑。
> 先读第 0 节把三者分清楚,再动任何东西。**

---

## 0. 三条并行的工作线 —— 谁在做什么

| 线 | 工作目录 / 标志 | 在做什么 | SLURM job 名前缀 |
|---|---|---|---|
| **窗口 A(本文件作者)= BDE 子课题** | `pipeline/bde/`、`dashboard/`、`pipeline/analysis/build_hard_set.py` | purge 后重建 BDE homo 描述符库 + B6 champion;分析(nitro / g-xTB 基线失败 / homo AL);hard-set 看板 | `bde_homoprod`(genoa) |
| **窗口 B = dG 主线 round10 主动学习** | `cross_benzoin/`、`data/cross_benzoin/cross_round10*`、`cross_round7/*recovered*` | round10 特征组装 + AL 打分选下一批 DFT;rounds1-7 恢复复现 | `assemble_r10feat`、`score_r10*`、`assemble_r17`、`train_r17`、`bde_cross`、`mordred_cross`(rome) |
| **NHC 项目(与 benzoin 无关)** | `/gpfs/scratch1/shared/schen3/workfow/nhc-*` | NHC 全局 SP / 主动学习 / crude 阵列 | `nhc-gsp-*`、`nhc_backup_cron`、`nhcpool-feat`、`orphan_cron` |

**分清窗口 A / B 的可靠办法**(分支名、文件时间戳都不可靠——共用 checkout):
1. `squeue -u schen3 -o "%.12i %.20j %.10P %Z"` 看 job 名 + WorkDir。`bde_homoprod` = A;
   `score_r10*` / `assemble_r10*` / `train_r17` = B;`nhc-*` / `*_cron` = NHC。
2. `ps -eo pid,ppid,lstart,cmd | grep -E "python|featurize|score_round|assemble_cross"`
   找 **PPID=1 且启动时间在最近几分钟** 的进程——那是某个窗口此刻活着的证据。
3. 已死的中断会话不会再起新进程;此刻在跑的就是别人的活的窗口,别碰它的文件。

**分工约定(用户 2026-09-02 确认 homo+cross+BDE 是同一个项目,但两个窗口分头做)**:
- 窗口 A 不推进 `cross_round{N}` 的采样/组装/AL 打分/`train_cross_*` —— 那是 B 的。
- 窗口 B 不重跑 `pipeline/bde/*`、不提大 genoa 阵列(A 的 `26316404` 在跑);cross 工作用 **rome**。
- 两边用 memory 文件 + 本文件 + 各自的 STATUS 文档做交接面,不直接改对方的工作文件。

---

## 1. 仓库 / 分支 / git 状态

- live 仓库:`/gpfs/scratch1/shared/schen3/benzoin-dg-restored`(= `/scratch-shared/...` 同一 inode)。
  旧的 `/gpfs/scratch1/shared/schen3/benzoin-dg` 是 purge 后的空壳,别用。
- 分支:`agent/recovery-20260902`。
- **`origin/agent/recovery-20260902` = `d1804ea`(purge 恢复 commit)。本地 HEAD 领先 8 个 commit,
  全部未 push。** 窗口 B 的 round10 工作还在工作区未 commit。
- **push 之前**:确认两个窗口的东西都进去了(A 的 8 个 commit + B 的 round10 文件)。
  建议由用户或最后收尾的窗口统一 push,避免分支竞态。

### 窗口 A 的 8 个 commit(本地,未 push)
| commit | 内容 |
|---|---|
| `6824063` | 采纳窗口 B/codex 的 `PY=` 覆盖 diff(36 文件)+ g-xTB 二进制路径修复 |
| `ca15188` | BDE `STATUS.md` 入口 + 产物 BDE 标签重拼 + featurize campaign 脚本 + `train_gnn_hybrid_bde.py` 路径修复 + nitro note |
| `94873ba` | 采纳窗口 B 的 cross rounds-1-7 恢复复现脚本 + manifests(未改写,原样入库) |
| `c375016` | 重建 chemprop/torch 环境 `/home/schen3/venv/bde_gnn`(torch 2.13 + chemprop 2.2)+ B6 submit 脚本改指 |
| `10f2c73` | `PROJECT_STATE_20260902.md`(全项目快照:补算 todo / 磁盘 / round11 / 各任务模型) |
| `7e1ebad` | homo dG 难尾巴 Boltzmann-relabel pilot 收集 → 标签杠杆第 2 个 null |
| `13862b3` | (a) 难尾巴 = g-xTB 基线失败 的诊断 + (c) hard-set 看板 `dashboard/` + `build_hard_set.py` |
| `44fde05` | `PROJECT_STATE` 并入 (a)(b)(c) |

### 工作区未 commit(窗口 B 的,别替它 commit 除非它明确交接)
```
 M cross_benzoin/slurm/submit_backup_recovery_artifacts.sh
?? cross_benzoin/slurm/submit_assemble_round10_fat_features.sh
?? cross_benzoin/slurm/submit_score_round10_active_learning.sh
?? data/cross_benzoin/cross_round10_fat20_stage1/         (大,含 chunk_*/ 已 gitignore)
?? data/cross_benzoin/cross_round7/train_ensemble_7rounds_recovered_v1/
?? data/cross_benzoin/cross_round10/smoketests/
?? data/cross_benzoin/homo_v6/aldehydes_all.partial42k.csv   (窗口 A 的本地安全副本,已 gitignore)
```

---

## 2. 窗口 A(BDE)当前状态 —— 从这里接续

**权威文档**:`pipeline/bde/STATUS.md`(子课题入口)、`PROJECT_STATE_20260902.md`(全项目)。

### 2.1 正在跑
| job | 内容 | 状态 / 下一步 |
|---|---|---|
| **`26316404` `bde_homoprod`** | genoa,2209-task 阵列(%96),`submit_bde_homo_product_featurize.sh` → `featurize_product.py --multiwfn --emit-aldehydes` over 全部 220,859 homo pair。一趟同时产出 `products_all.csv` + `aldehydes_all.csv` 两个描述符库 | ~128/2209(2026-09-02 18:00);genoa 竞争,ETA 视吞吐 ~1–2 天。**完成后**见 2.2 |

### 2.2 阵列完成后的确定动作(按顺序)
1. `python pipeline/bde/assemble_homo_descriptor_libs.py --chunk-dir data/cross_benzoin/bde_homo_product_featurize_20260902`
   → 写 `data/cross_benzoin/homo_v6/{products_all.csv, aldehydes_all.csv}`(canonical-SMILES→library-index id 重映射;partial 42k 兜底)。
   ⚠️ 这会 **覆盖** 现在的 42,335 行 `aldehydes_all.csv`。窗口 B 的 round10 组装 / rounds1-7 复现
   届时应对着完整库**重跑**(join 率会从 40,960/42,335 升上去,rounds1-7 表的 32,418→应接近历史 32,456)。
2. **inode 清理**(重要,见 `PROJECT_STATE §2`):
   `find data/cross_benzoin/bde_homo_product_featurize_20260902 -type d \( -name xyz -o -name ald_xyz \) -exec rm -rf {} +`
   回收 ~40 万 inode(scratch inode 用量已 48.8%)。`.xyz` 对训练无用。
3. B6 scaffold-disjoint 重训(带 checkpoint):
   `sbatch pipeline/slurm/submit_b6_scaffold_disjoint_ckpt.sh`(2×H100 阵列,`PY=` 已指向新
   `venv/bde_gnn`;`H` 已 repo 相对解析)。产出 `runs/logs/scaffold_disjoint_bde/models/b6_{aldehydes,products}_scaffold_disjoint.pt` +
   `..._ckpt_result.json` + `..._ckpt_pred.csv`。**期望复现**:醛 MAE ~1.58、产物 ~3.06(scaffold-disjoint)。
4. 结果 `.pt` / JSON / pred CSV **全部 `git add -f`**(gitignore 排除 `*.pt` / `runs/`),
   并加进 `submit_backup_recovery_artifacts.sh` 归档清单(purge 教训:gitignore + 无备份 = 一次策略变更就没)。
5. 重跑 `python pipeline/analysis/build_hard_set.py` → hard-set 会自动补上 BDE 醛/产物两个 source
   (现在是 stub);`streamlit run dashboard/hard_set_app.py` 的 Progress 页开始有对比点。

### 2.3 已完成的分析结论(都已 commit + 写进 memory / notes)
- **homo dG 难尾巴 = g-xTB 基线失败**,不是模型/标签问题。150 个最差 P/磺酰基/亚胺/酰胺分子:
  |g-xTB 基线误差| mean|·| 13.9 kcal(全库 4.3),corr(残差,基线误差)=0.888。
  → 推理时按子结构(P=O / 鏻盐 / 多磺酰基 / 多酰胺)**强制 route_to_dft**。
  `pipeline/analysis/notes/gxtb_baseline_failure_hardtail_20260902.md`。
- **homo 主动学习不会提升**:pool-AL 不适用(库已全标注);难尾巴 Boltzmann 重标是 2× null
  (冻结 MAE 10.01→10.13,boltz_corr ⟂ 残差)。`memory/homo-active-relabel-null-result`。
- **nitro Δ_SP "双峰" = 3/18 个 g-xTB 自身失败**(醛 BDE >100 kcal/mol),不是 level-of-theory 双峰。
  `pipeline/bde/notes/nitro_delta_sp_bimodal_20260902.md`。
- **BDE 各任务冠军仍是 B6**(D-MPNN + H-SPOC 局部描述符 `x_d` 融合):scaffold-disjoint
  醛 MAE 1.579、产物 3.060。详见 `pipeline/bde/STATUS.md`。

### 2.4 未做的 BDE 侧待办
- 推理时 substructure→route_to_dft veto 规则(零训练成本,见 2.3 第一条)。
- 醛 formyl C–H BDE 的 `qc.py` 物理上限收紧(去 g-xTB 毒标签,~105–110)——**别直接改共用 qc.py**,
  单独前后对照。
- `products_all.csv` 会缺 `dG_gxtb_kcal` 列(`featurize_product.py` 出的是 ALPB `dG_xtb_kcal`);
  B6 BDE 训练用不到,但 `headroom_probe.py` 等分析脚本需要——需要时从 `products_bdfe_gxtb_descriptors.csv` 回填。

---

## 3. 窗口 B(dG 主线 round10)当前状态 —— 别替它推进,只供参考

- `train_r17`(`26314357`)已完成:rounds1-7 恢复复现**通过**(CV MAE 1.877 vs 历史 1.883,
  全部落在噪声内)。`memory/rounds17-reproduction-confirmed`。**rounds 1-7 现在是可信基线。**
- round10 特征组装完成(`assemble_r10feat` `26318983`):
  `data/cross_benzoin/cross_round10_fat20_stage1/cross_round10_fat20_stage1_features_..._merged.parquet`
  (15,983 行 × 583 列)。
- **`score_r10`(`26320177`)运行中(rome)** —— round10 的 AL 打分,`score_round_active_learning.py`,
  按 ensemble prediction-std 选下一批 DFT-SP。**这是窗口 B 的活,不要碰。**
- rounds 8-9 的 DFT-SP 标签 purge 中丢失、无副本;几何可能在
  `/home/schen3/benzoin_backups/cross_benzoin_xyz_archive/cross_round{8,9}_xyz_geometry.tar.gz`
  (若完整,只需重跑 SP)。要不要补是决策题(~1.3 万核时)。

### 窗口 A 给窗口 B 的输入(已写进 `PROJECT_STATE §3`,B 自行取用)
hard-set 显示 cross-dG 最差残差集中在 `flexible` > `fg:amide` > `gxtb_baseline_failure`(667)
> `fg:sulfonyl`;含磷已被现有 sampler 过采样所以在 hard-set 里反而少。**若做 round11**:
- 加 amide-heavy / high-flexibility strata(现在只有磷单列);
- `score_round_active_learning.py` 现在只按 epistemic uncertainty 排 —— 基线失败分子可能
  ensemble std 很低(自信地错),应额外按预测 |dG_gxtb − dG_orca| 排一路并入选池;
- 这类基线失败子结构在推理时强制 route_to_dft。
- learning curve(round1-9)MAE 1.842→1.797→1.770(边际收益每翻倍减半),round11 平均增量
  <0.02 —— 除非 round10 实测点偏离趋势,否则默认不做,补 round8/9 标签更值。

---

## 4. 环境 / 资源 / 安全

- **Python 环境**(shared `envs/*` 全被 purge 清空):
  - 表格 ML / 特征化 / 分析 / **streamlit**:`/home/schen3/venv/nhc-workflow/bin/python`
  - torch / chemprop(B6 GNN):`/home/schen3/venv/bde_gnn/bin/python`(2026-09-02 新建,torch 2.13 + chemprop 2.2)
  - g-xTB:`/home/schen3/xtb/bin/xtb`(`--gxtb`,是 g-xTB-capable);Multiwfn `/home/schen3/mutiwfn/Multiwfn_noGUI`
- **磁盘**(`/usr/hpc/bin/myquota`,`quota -s` 会 hang):home 空间 42% / inode 40%;
  **scratch 空间 12.7% / inode 48.8%** —— inode 是唯一要盯的。窗口 A 的阵列跑完
  `.xyz` 会加 ~44 万文件,**assemble 后必须按 2.2 步骤 2 清理**。
- **SLURM**:窗口 A 用 genoa,窗口 B 用 rome,NHC 用 fat_rome/rome。genoa 现在 ~94% 占满。
  别在 `26316404` 跑着的时候再提大 genoa 阵列。
- 本地后台进程(窗口 A 起的,会随会话结束消失):monitor `monitor_bde.sh` —— 每 ~2h 汇报 +
  阵列出队即触发;`build_bde_gnn_env.sh`(已完成)。

---

## 5. 关键文档索引

| 文档 | 作用 |
|---|---|
| **本文件** | 三条工作线的分工 + 两窗口的 git/job 状态 |
| `PROJECT_STATE_20260902.md`(根) | 全项目快照:补算 todo、磁盘、round11、各任务模型表现、hard-set |
| `pipeline/bde/STATUS.md` | BDE 子课题权威入口(排名、purge 资产盘点、恢复计划) |
| `pipeline/bde/PROGRESS_20260714.md` | BDE 逐日工作日志(结论以 STATUS.md 为准) |
| `RECOVERY_REPORT_20260902.md`(根) | 2026-07 purge 恢复全过程 |
| `cross_benzoin/docs/HANDOFF_round10_20260721_ZH.md` | round10 配方 + learning curve(窗口 B 的线) |
| `cross_benzoin/docs/STATUS_{ZH,EN}.md` | dG 主线 living status(较旧,有 2026-07-17 勘误) |
| `dashboard/README.md` | hard-set 看板怎么跑 |
| memory `~/.claude/.../memory/*.md` | `concurrent-bde-session-20260902`、`concurrent-cross-bde-session`(已更新)、`homo-active-relabel-null-result`、`bde-post-purge-asset-state`、`rounds17-reproduction-confirmed`、`benzoin-dg-repo-moved-and-data-loss`、`home-backup-archives-recovery`、`purged-binaries-and-envs`、`scratch-disk-quota-risk` |

home 备份镜像:`/home/schen3/benzoin_backups/docs_snapshot_20260902/`(含 memory 镜像)。

---

## 5b. 窗口 A 追加进展(2026-09-02 晚,sonnet-5 续会话)

本地又多了 **2 个 commit**(现 origin 落后 **11** 个,仍全部未 push):
- `826c0e9` **float-id 修复**:purge 恢复把醛侧 label / scaffold-split / alfabet CSV 的
  id 写成 `2.0` 浮点,而重建的 `*_all.csv` 用 `2` → 字符串 merge 静默 join 到 ~0 行。
  加 `qc.norm_id()`,在 4 个 BDE trainer 每个 id 列 read 后套用(**没改共用 CSV**)。
  ⚠️ **给窗口 B**:如果你的 round10 / rounds1-7 组表也 join 到这几个醛侧 CSV 的 id,
  留意同样的 `2.0` vs `2` 问题(`train_r17` 已过说明你那条路没踩到,但新代码注意)。
- `ea7936e` **阵列后 model sweep 前置**:`pipeline/bde/POST_ARRAY_MODEL_SWEEP.md`(计划)
  + `archive_completed_chunk_geoms.sh` + `submit_b6_deep_ensemble.sh` +
  `aggregate_b6_ensemble.py` + `gbm_bakeoff_hspoc.py` + 修好的 `submit_b4_b5_scaffold_disjoint.sh`。

新起的后台/监控(会随本会话结束消失,下个会话按需重起):
- **staging job `26324608`** `bde_geomarch` = 增量几何归档,取代 §2.2 step 2 的 `rm -rf`:
  完成的 chunk 的 xyz → `chunk_XXXX/geom.tar.zst`(校验后删散文件),阵列出队后做最后
  一遍并把所有 geom tar 合并进 `~/benzoin_backups/bde_homo_rebuild_20260902/` 再自退。
- Monitor task `bm8632d2g` 盯 `26316404` + `26324608`,每 30min 报进度、出队即通知。

已加的 home 备份:`~/benzoin_backups/bde_homo_rebuild_20260902/`
(products/aldehydes BDE label CSV、partial 醛库、scaffold split、float-id 原件)。

GBM bake-off 首screen(partial 42k):HistGradientBoosting 比现任 XGB 稳定低 ~3–4% MAE
→ 全量重跑带上它。

---

## 5c. scratch1 INODE incident + 隔夜自动化(2026-09-03 00:30–01:30,用户睡了让我自主推进)

**incident**:scratch1 inode 配额冲到 ~116%(4.6M / 4.0M 硬上限)。**元凶不是 BDE 阵列**
(~62k),是 `/gpfs/scratch1/nodespecific/<node>/schen3.<jobid>[_<task>]` 里被 **抢占**
(genoa 挤,我们的低优先级 array 频繁 requeue)/ 杀掉的 task 残留的 per-node 工作目录
(xtb/Multiwfn scratch)。`/scratch-local` 就是 `/gpfs/scratch1/nodespecific/int5`,算同一个
配额。横跨 BDE + 窗口 B + NHC。GPFS grace 暂时还在扛,job 没崩,但很险。

**已做(用户授权了对 nodespecific 的操作 + scontrol hold;但 `scontrol hold` 被集群拒,
throttle 也拒)**:
- `pipeline/bde/clean_orphan_nodespec_workdirs.sh` 账号级孤儿清理器,**并行删**(xargs -P16),
  keep 规则:仅当 `<arrayjob>_<task>` 正在 **该节点** RUNNING、或目录 25 分钟内动过 才保留;
  其余全删。作为常驻 janitor 跑在 staging(job `26326282`,LOOP=1,每 15 min 一轮)。
  commit `b9a2ec5` 起,后又收紧了 keep 规则(见文件头注释)。
- 一次性并行清理把 inode 从 116% → ~106%(NHC/其他 job 的孤儿清掉了);剩下的主要是
  **我的两个 array 的抢占孤儿**,旧 keep 规则(“array 还活着就整个豁免”)碰不到 → 已收紧。
- geom 归档器换 5 min 一轮(`26325270`),持续回收 chunk 的 xyz。

**隔夜自动化(SLURM,不依赖会话存活)**:
- `26326282` 常驻孤儿 janitor(staging)。
- `26325270` geom 归档器(staging)。
- **`26326313` `bde_post_sweep`**:`--dependency=afterany:26316404:26324801`,两个 featurize
  阵列都结束后自动跑:完整度门槛(≥97% chunk 有完整 features.csv,否则 exit 3 不组装)→
  `assemble_homo_descriptor_libs.py` → 备份两个库 → 提交 model sweep(B6 ckpt + B6 deep
  ensemble + 聚合 + B4/B5 + 全量 GBM bakeoff)。脚本:`pipeline/bde/submit_post_array_sweep.sh`。
- 会话里的 Monitor:array 进度 2h 心跳 + inode>114% 告警 + 写失败 canary。

**接手要看的**:`squeue -u schen3`;`myquota` 看 inode;`slurm_logs/clean_orphans_*.out`、
`bde_post_sweep_*.out`。若 inode 还往上冲(>114%),janitor 没跟上 → 考虑 `scancel` rome
`26324801` 的 pending 段(running 的留着跑完自清)。sweep 结果落 `runs/logs/scaffold_disjoint_bde/`,
跑完记得 `git add -f` + 更新 STATUS 排名 + 重跑 build_hard_set。

commits 追加:`b1656b4` `b9a2ec5`(孤儿清理器)、`<sweep driver + path fix>`。分支仍未 push。

---

## 6. 一句话 next action

- **窗口 A 接手**:等 `26316404` 完成 → 跑第 2.2 节 5 步(assemble → inode 清理 → B6 重训 →
  `git add -f` 结果 → 重建 hard-set)。
- **窗口 B 接手**:继续 `score_r10` 之后的 round10 流水线(DFT-SP → 组表 → 重训 → learning curve 第 4 点);
  round10 完成后对着窗口 A 重建的完整 `aldehydes_all.csv` 重跑组装。
- **要 push**:先确认两窗口的 commit / 未 commit 文件都收齐,再统一 push `agent/recovery-20260902`。
