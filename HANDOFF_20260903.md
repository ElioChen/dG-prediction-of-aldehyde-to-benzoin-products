# 交接文档 — 2026-09-03（窗口 B：cross-benzoin dG + r8/9 恢复 + BDE 恢复）

> 写给下一个接手的会话。**先读第 0-1 节做立即行动，再读第 3 节重建监控。**
> 并行的 NHC 窗口（schen3-bc）和 BDE 自动化 job 不要碰（第 2 节标了）。
> 本文档结构是**固定套路**，见第 8 节 —— 下次交接照着写。

---

## 0. 一句话

> **09-03 ~19:00 自主模式改写**（用户："有序推进，很长一段时间不给指令"）。
> 下面是权威现状，旧的 §1.2/§1.3 步骤已被自动化链取代，仅供参考。

**r8/9 DFT 标签恢复已全自动化，无需人工干预到 r1-9 模型为止：**

- **Phase A 产物 SP**：✅ 完成（r8 7999/8003 + r9 12929/12931，失败率 <0.1%）。
- **Phase B 醛几何重生 `26351006`**：✅ 完成（413/413，0 fail）。发现并补了一个
  27-chunk 缺口（task 15-41，job `26353929`，resume-guard 只数行不看 error 的 bug，
  已修 commit `e994ffd`）。最终 geom list = **20,564 个 error-free 醛**。
- **醛 r2SCAN-3c SP 阵列 `26354346`**（0-205 `%110` fat_rome）：🏃 在跑，ETA ~2h。
- **`26354407`**（`submit_r89_assemble_after_ald_sp.sh`, `afterany:26354346`）：醛 SP
  出队后自动 → `assemble_r89_dft_sp.py --rounds 8 9` + 刷新 home 备份 + `sbatch`
  下一步。
- **`submit_r19_retrain_chain.sh`**（`26354407` 自动提）：r1-9 重训 steps 1-4
  （组表→relabel→剪枝→`train_scaffold_disjoint`，nequip env）+ 自动提 r1-9 GNN。
- **`gnn_attn_9r_cpu`**：r1-9 attentive GNN 重训（CPU/fat_rome，~数小时）。
- **自主终点** = r1-9 champion+ensemble+GNN 出结果。**round10 重打分 / round10 DFT
  是用户决策点，到此停，不要自动往下**（见 §1.4）。

**接手时只需**：`squeue` 看链跑到哪 → `tail RUN_LOG` → 按 §3 重建监控 → 若某环节
FAILED，手动从 `run_r19_retrain_chain.sh` / `submit_r19_retrain_chain.sh` 续。

**已清的坑**：purge 丢的 `candidates_v3/aldehydes_with_scaffold_split.parquet` 已用
`cross_benzoin/rebuild_aldehydes_with_scaffold_split.py` 重建（220,524 行，80/10/10）。

---

## 1. 立即行动

### 1.1 先对齐现状（每次接手都做）
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.20j %.9P %.2t %.10M %R" | grep -vE "nhc-|_cron|orphan"
/usr/hpc/bin/myquota | grep -A3 wstor_scratch1        # inode，>60% 才需要动手
git log --oneline -3 ; git status --short             # 见第 4 节（git 会间歇卡）
tail -50 RUN_LOG_20260903.md                          # 上个会话的逐步日志
```

### 1.2 Phase A 产物 SP 收尾（几乎已完成）
- 阵列 `26340036`（r8）/ `26340037`（r9）出队后：
  ```
  python3 -c "import glob,csv;print(sum(1 for f in glob.glob('data/raw/dft_sp_cross/cross_round8/sp_products/chunk_*.csv') for r in csv.DictReader(open(f)) if r.get('E_orca_Eh')))"
  # 期望 r8 ~7997, r9 ~12760 (少数几何持续失败，可接受)
  ```
- **失败行重试**：`orca_sp_from_geomlist.py` resume-safe，空 `E_orca_Eh` 的行重跑会重试。
  若失败率 >2%，对失败的 chunk 重提一次阵列（同命令，同 `--out-csv`）。

### 1.3 Phase B 醛腿（**主线**）
1. 等 `26351006` 出队（本会话监控 `bis3t3nyc` 会通知；或 `squeue -j 26351006`）。
   期望 ~413 chunk × ~50 醛，产出 `data/cross_benzoin/r89_aldehyde_regen/chunk_*/`
   下 `aldehydes.csv`（`id` 列 = clean_v6 数字 lib_id，含 `G_xtb` `xtb_energy`）+ `xyz_ald/`。
2. **建醛 SP geom list**（写个小脚本或 inline）：遍历 `chunk_*/aldehydes.csv` 里 `error` 空、
   `G_xtb` 非空的行 → `id,xyz_path`（xyz_path = `chunk_XXXX/xyz_ald/<对应文件>`；
   文件名规则看 `aldehydes.csv` 的 `xyz_file` 列，直接用那个绝对路径）→
   写 `data/cross_benzoin/r89_aldehyde_recover/aldehyde_geom_list.csv`；
   同时写 `aldehyde_thermal.csv`（`id, thermal_ald_Eh = G_xtb - xtb_energy`）。
3. **提交醛 SP 阵列**（复用 `cross_benzoin/slurm/submit_r89_sp_array_fatrome.sh`）：
   ```
   REPO=/gpfs/scratch1/shared/schen3/benzoin-dg-restored
   LIST=$REPO/data/cross_benzoin/r89_aldehyde_recover/aldehyde_geom_list.csv
   OUT=$REPO/data/raw/dft_sp_cross/r89_aldehyde_sp
   N=$(( $(wc -l < "$LIST") - 1 )); CHUNK=100; NT=$(( (N+CHUNK-1)/CHUNK )); mkdir -p "$OUT/logs"
   sbatch --array=0-$((NT-1))%60 --output="$OUT/logs/%A_%a.out" \
     --export=ALL,REPO="$REPO",GEOM_LIST="$LIST",OUTDIR="$OUT",CHUNK=$CHUNK \
     cross_benzoin/slurm/submit_r89_sp_array_fatrome.sh
   ```
4. **组装**（产物 SP + 醛 SP 都完成后）：
   ```
   /home/schen3/venv/nhc-workflow/bin/python cross_benzoin/assemble_r89_dft_sp.py --rounds 8 9
   ```
   注意：`assemble_r89_dft_sp.py` 现在从 `data/raw/dft_sp_cross/r89_aldehyde_sp/chunk_*.csv`
   读醛 E_orca，key = `id`（数字 lib_id）；从 `r89_aldehyde_recover/aldehyde_thermal.csv`
   读醛 thermal。产物侧从 `cross_round{r}/sp_products/` + `cross_round{r}_recover/product_thermal.csv`。
   **⚠️ 若醛 SP 表 key 是别的（比如 InChIKey），改 assemble 脚本的 join 或加一层 lib_id↔ik 映射。**
   产出 `data/raw/dft_sp_cross/cross_round{8,9}/cross_round{8,9}_dft_sp.csv`（`id, dG_orca_kcal`）。
5. **组 r1-9 表 + 重训**（round10 handoff §4.8-4.13 的配方）：
   `assemble_cross_training_table_v3.py --rounds 1..9`（product-mordred 路径见那份文档）→
   `relabel_scaffold_split` → `prune_table_to_champion_features.py` → `train_scaffold_disjoint.py`。
6. **r1-9 GNN 重训**（这次才是真正的测试，数据规模够了）：
   `cross_benzoin/slurm/submit_gnn_attentive_7rounds_recovered_cpu.sh` 为模板，换成 r1-9 表 +
   r1-9 champion-dir。env = `/home/schen3/venv/nequip`（已装 joblib/sklearn/xgboost/pyarrow）。
   GPU 排队卡 fairshare → 直接用 CPU/fat_rome（模板已是）。

### 1.4 round10 —— **用户 09-04 09:00 接手点**

**过夜全链已跑完**（09-04 03:45）。经过：
- scratch1 inode 09-03 22:35 爆到 133%（BDE geomarch 积压，非本窗口，b1 清）→ 09-04
  04:00 回到 95%。期间链卡一次（assemble step-3 sbatch 被 EDQUOT 吞），01:05 手动补提。
- 修了 3 个 purge 后脚本漂移 bug：GNN `--ensemble-path` 文件名、09-01 旧 r1-9 目录、
  `prune` META 缺 `new_scaffold_split`（commit `de9f55d` / `272bfef`）。

✅ **r1-9 champion+ensemble**（`cross_round9/scaffold_disjoint_9rounds_v1`）：
   scaffold-disjoint holdout n=448 —— ensemble MAE **2.254** (R² 0.771)，XGB 2.544，
   g-xTB baseline 5.037，clean-train CV XGB MAE 1.993。
✅ **r1-9 GNN + blend**（`cross_round9/gnn_attentive_9rounds_v1`）：GNN-only 2.313，
   **blend (w_gnn=0.40) MAE 2.167**。bootstrap（`bootstrap_significance.json`）：
   delta 0.086，90% CI [0.032, 0.140]（不含 0），**P(blend>ensemble)=0.9954**。
   → GNN 在 r1-9 规模上显著有效（对比 r1-7 recovered 的 w_gnn=0 null）。
✅ **round10 重打分**（用 r1-9 表）：新 `cross_round10_fat20_stage1_dft_selection.csv`
   （09-04 03:43，2000 对，取代旧的 r1-7 版）。reaction_type：carbo-hetero 787 /
   carbo-carbo 484 / hetero-hetero 419 / aliph-carbo 162 / aliph-hetero 129 / aliph-aliph 19。
✅ **round10 产物 SP `26364157`** —— DONE，**1994/2000 E_orca**（6 empty）。
   `data/raw/dft_sp_cross/cross_round10_fat20_stage1/sp_products/`。
✅ **round10 醛 SP `26365893`**（32 task %110）—— DONE 08:51，**3166/3172 E_orca**（6 empty）。
   `data/raw/dft_sp_cross/r10_aldehyde_sp/`。醛 thermal +几何 list 在
   `data/cross_benzoin/r10_aldehyde_recover/{aldehyde_thermal.csv,aldehyde_geom_list.csv}`。
   （3,172 = selection 2000 对的 3,804 个醛里、扣掉 608 个已有 r89 SP、再扣 ~24 个
   regen straggler；缺的 ~0.9% 会让极少数 pair 组不出 round10 标签，可后补。）
   **→ round10 三腿 SP 全齐，可直接组装 round10 dG 标签。**

**你要做的**（09:00 起）：
1. 读 `cross_round9/gnn_attentive_9rounds_v1/bootstrap_significance.json`（上面已摘），
   决定 r1-9 blend 是否作为新 champion。
2. `sacct -j 26364157` 看 round10 SP 进度。出队后：需要**醛 SP** —— round10 的醛可能
   不在 r89 醛库里（round10 是新 candidates）。检查 `round10_sp_geom_list.csv` 的
   product id 对应的 donor/acceptor 是否已有 DFT 醛 G；缺的走一次醛 SP（仿 r89 醛腿）。
3. 三腿组装 round10 标签 → `assemble` 逻辑（产物几何在
   `cross_round10_fat20_stage1/chunk_*/xyz_prod`）→ 组 round1-10 表 → 视情况再训。
4. round10 的 **N / 是否再来一轮 AL / 要不要新 candidates** 是你的判断。自动化止于 top-2000 SP。
   BDE 被 b1 降速了（`26316404` 40→8 / `26324801` 100→15），inode <90% 时调回
   （`scontrol update JobId=... ArrayTaskThrottle=...`）—— 那是 window A 的事。

---

## 2. 在跑的 SLURM 作业（09-03 ~19:00）

| jobid | 名 | 干什么 | 状态 | 完成后 |
|---|---|---|---|---|
| `26340036/37` | `r89_sp` | Phase A 产物 SP | ✅ DONE 17:08 | — |
| `26351006` | `r89_ald_regen` | Phase B 醛几何重生 | ✅ DONE 18:32 | — |
| `26353929` | `r89_ald_regen` | 醛几何 task 15-41 缺口补跑 | ✅ DONE 18:44 | — |
| **`26354346`** | `r89_sp` | **醛 r2SCAN-3c SP 阵列（0-205 %110 fat_rome，20,564 醛）** | 🏃 ~2h | `26354407` 自动接 |
| **`26354407`** | `r89_assemble` | `afterany:26354346` → assemble 标签 + home 备份 + 提 retrain | PD Dependency | 自动 |
| ↳ `r19_retrain` | `submit_r19_retrain_chain.sh` | `26354407` 自动提 → r1-9 重训 steps 1-4 + 提 GNN | 待提 | 自动 |
| ↳ `gnn_attn_9r_cpu` | — | r1-9 attentive GNN 重训 | 待提 | **停，round10 = 用户决策** |
| `26316404/24801` | `bde_homoprod` | BDE 220k featurize | 🏃 ~50%，ETA ~9/4 | `26326313` 自动 |
| `26326313` | `bde_post_sweep` | BDE assemble + B6 sweep | PD Dependency | 自动，人只做 §POST_ARRAY step6 |
| `26348174/75` | `bde_geomarch`/`clean_orphans` | BDE 归档 + nodespec 清理 | 🏃 | 自退/常驻 |

**不是我的、别碰**：`26350401 mace-polar-val`、`int2-bres-study`、所有 `nhc-*` / `*_cron`（NHC 窗口 schen3-bc）、
BDE 自动化（`26316404/24801/26313/48174/48175`）。

---

## 3. 会话内监控（**已随上个会话结束失效，必须重建**）

用 Monitor 工具重建这几个（persistent，timeout 3600000）。**09-03 19:00 版**（旧的 1-2 已失效）：

1. **醛 SP 阵列 `26354346` 出队** → 核对 `26354407` assemble 起跑。
   轮询 `squeue -j 26354346`，空时报 E_orca 行数 + `26354407` 状态。
   （本会话 = `b5ktfkdbg`。）
2. **retrain 链推进**：`26354407` 出队后核对 `r19_retrain` job 起跑；再核对
   `gnn_attn_9r_cpu` 起跑。检查 `data/raw/dft_sp_cross/cross_round{8,9}/cross_round{8,9}_dft_sp.csv`
   有内容、`data/cross_benzoin/cross_round9/scaffold_disjoint_9rounds_v1/` 出现。
3. **BDE 阵列 `26316404,26324801` 出队** → 核对 `26326313`（不干预，只观察）。本会话 = `botd4dtq0`。
   本会话另有 `bsmqchp7x` = retrain 链（r19_retrain/gnn_attn_9r/r19_postgnn）状态转换 + 失败告警。
4. **60min RUN_LOG 快照**（aldSP 行数 / r8-9 labels / 链状态 / r19 champ / bootstrap / inode）。
   本会话 = `ba5nh325z`（旧 `br6yk0gd7` 已停，内容过时）。
5. **2h digest cron**：`89b47fcc`（`CronList` 可见），每 2h 发合并状态 + 执行任何刚解锁的步骤。
   session-only，7 天过期。新会话重开要 `/loop 2h ...` 重建。

上个会话的具体命令在 `RUN_LOG_20260903.md` 和 Monitor 历史里；照抄改 jobid 即可。

---

## 4. 未提交的 git 改动

**git 在这个 GPFS mount 上间歇性卡死**（commit 挂 >2min 然后死，留 `.git/index.lock`；
`rm -f .git/index.lock` 后重试，GPFS 负载低时能过）。

- HEAD = `f675494`（本文件 + GXTB 修复 + build_r89 修复 + RUN_LOG 都已 commit，16:50 落地）。
  `git log` 从 pack 读快；`git status` 会卡，别 block 在它上面。
- **工作区应该干净**（除非新会话又改了东西）。下次改动照第 8 节套路 commit。
- `data/cross_benzoin/r89_*` 是数据（gitignored 或太大），不 commit；重要产物加进
  `cross_benzoin/slurm/submit_backup_recovery_artifacts.sh` 的归档清单。
- 分支 `agent/recovery-20260902`，领先 origin 很多，**全部未 push**（等两窗口收齐统一 push）。

---

## 5. 关键决策 & 不要重新讨论的事

- 用户 2026-09-03 定：round10 走「**r8/9 恢复 → 重组 r1-9 → 用 r1-9 重给 round10 打分 →
  round10 DFT**」全序列；**所有 cross GNN `.pt` 都要重训**。
- r8/9 恢复 = **三物种全重算**（`dG = G(prod) − G(donor) − G(acc)`）。产物几何来自
  `~/benzoin_backups/cross_benzoin_xyz_archive/cross_round{8,9}_xyz_geometry.tar.gz`（已解包到
  `data/cross_benzoin/cross_round{8,9}/chunk_*/xyz_prod/`）。**醛几何 = 重新生成**（不 harvest
  Window A —— 太慢/脆/gated；重生自包含且一趟出 thermal）。
- 醛侧 r2SCAN-3c SP 缓存 `data/raw/dft_sp_funnelv3/` purge 时丢了、无备份 —— 所以才要重算醛 SP。
- GNN r1-7 = null（`w_gnn=0`，gnn-only 2.368 vs ensemble 1.560）**不是 bug**，是数据规模问题。
- 所有大计算走 **fat_rome**（无 per-user QOS 上限），避开 genoa/rome 的 128 QOS（BDE 阵列 + NHC 在抢）。
- GPU 排队 = 账号 **fairshare 触底**（EffectvUsage 0.88）→ GNN 直接跑 CPU/fat_rome。

---

## 6. 已知陷阱

- **`cb_featurize.py` 硬编码 purge 掉的 g-xtb 路径**为 `GXTB_BIN` 默认 → submit 脚本必须
  `export GXTB_BIN=/home/schen3/xtb/bin/xtb` + `GXTB_SOLV="cosmo dmso"`。
- **ORCA 6.1.1**：`%scf LeanSCF false`/`NoLeanSCF` 硬失败；`orca_leanscf_mpi` SIG11 是瞬时的
  → 重试即可，别动 SCF 关键字（NHC 窗口确认）。
- **Window A `aldehydes.csv` 的 `index` 列 = `lib_id % 100`**（chunk 内位置 = geom 文件号），
  不是全局 id。`pairs.csv` 的 `lib_id` 才是全局 clean_v6 行号。
- **共享的 `clean_orphan_nodespec_workdirs.sh`**：grace 检查已改成看整棵子树最新 mtime
  （NHC 的 `schen3.<jobid>/nhc-global-sp-*/` 布局需要）。别退回顶层-only。
- **`staging` 分区对本账号 fairshare 触底** → janitor/geomarch 排不上，已迁 fat_rome。
- purge 恢复的醛侧 CSV 有 `"2.0"` 浮点 id → string merge 会 join 到 ~0 行（`qc.norm_id`，commit `826c0e9`）。
- inode 整天在 27-48% 震荡（crisis 是 116%）。>60% 才动手：查 `26348175` janitor 在扫、
  手动跑一次 `DRY=0 LOOP=0 bash pipeline/bde/clean_orphan_nodespec_workdirs.sh`（后台，find 很慢）。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **本文件** | 今天的交接（固定套路，见第 8 节） |
| `RUN_LOG_20260903.md` | 今天的逐步日志（脚本 / jobid / 决策 / bug）—— **living doc，边做边更** |
| `HANDOFF_20260902.md` | 昨天的交接 + §7 是今天的续（三条工作线分工、窗口 A BDE 状态） |
| `RECOVERY_REPORT_20260902.md` | 2026-07 purge 恢复全过程（cv_predictions 标签恢复、home 归档） |
| `cross_benzoin/docs/HANDOFF_round10_20260721_ZH.md` | round10 完整配方（§4.x 步骤，learning curve） |
| `pipeline/bde/STATUS.md` | BDE 子课题入口（窗口 A） |
| `pipeline/bde/POST_ARRAY_MODEL_SWEEP.md` | BDE 阵列后 model sweep 计划（`26326313` 跑的） |
| memory `~/.claude/.../memory/*.md` | `benzoin-dg-repo-moved-and-data-loss`、`rounds17-reproduction-confirmed`、`purged-binaries-and-envs`、`cross-round10-fat20-stage1-recovered`、`scratch1-inode-nodespec-orphans` 等 |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit -F <msg 文件>`。
   git 卡就在本文件第 4 节精确写清「已 staged 什么、msg 在哪、接手第一件事 commit」。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根），用本文件这套小节结构：
   `0 一句话` / `1 立即行动`（带确切命令）/ `2 在跑的 SLURM 作业`（表格 + 标注别碰的）/
   `3 会话内监控`（**强调随会话失效，给重建命令**）/ `4 未提交 git` / `5 关键决策` /
   `6 已知陷阱` / `7 文档索引` / `8 本节（套路自身，复制过去）`。
3. **`RUN_LOG_<YYYYMMDD>.md` 收尾**：最后一行进度快照 + 任何半截状态。
4. **更新 memory**：`MEMORY.md` 里指向新 HANDOFF 的一行；有非显然的新事实写成 memory 文件。
5. **产出 resume prompt**（贴给新会话的指令），模板见下。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（窗口 B：cross-benzoin dG + r8/9 恢复 + BDE 恢复）。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_<date>.md，第 0-1 节是立即行动。
2. 会话内监控全部随上个会话失效了 —— 按第 3 节用 Monitor 工具重建。
3. squeue -u schen3 对齐第 2 节作业表；确认哪些完成了，做第 1 节的收尾。
4. 并行的 NHC 窗口(schen3-bc)+ BDE 自动化 job(26316404/26324801/26326313/26348174/26348175)
   和 mace-polar-val 不要碰。
5. 进展记进 RUN_LOG_<date>.md；阶段性更新 HANDOFF；会话结束按第 8 节套路交接。
```
