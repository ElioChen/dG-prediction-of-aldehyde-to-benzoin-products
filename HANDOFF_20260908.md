# 交接文档 — 2026-09-08（自主夜跑后，用户接管）

> 写给下一个接手的会话。**先读第 0-1 节。** 本会话（09-07 晚 → 09-08 晨）在用户授权下
> 自主推进建议 1（B97-3c 便宜基线杠杆）。只有 NHC（`schen3-bc`/`schen3-7a`）、
> `mace-delta-train`、`orcasp_INT2_*` 不碰。本文档结构固定，见第 8 节。

---

## 0. 一句话

**champion 仍是 r1-10（blend MAE 2.215，未变）。** 本会话把 handoff §1.2 的**建议 1
（B97-3c 便宜基线杠杆）从"未验证的 pilot 假象嫌疑"推进到"端到端验证的真实精度杠杆"**：

1. **生产几何复核**（job 26454510，128 对 pilot holdout，在真实 funnel_v3 + GFN2-ohess
   几何上重算 r2SCAN-3c / B97-3c / g-xTB SP）→ **判据 GREEN**：`resid_b973c` std
   **1.01**（1980 对上 0.94）vs g-xTB **3.10**，比值 **0.33**（pilot 一次性几何 0.26），
   control 与 hetero-hardtail 两组一致。pilot 的 0.26 **不是**一次性构象假象。
2. **A/B 代理**（job 26456516，1999 对 = 448 holdout + 1551 分层 train，同一重生成
   切片上 single-XGB Δ-model 各重训一次）→ **决定性 POSITIVE**（train 1532，5 seeds）：

   | 目标 | 基线 | raw MAE | Δ-model holdout MAE |
   |---|---|--:|--:|
   | 自洽 `dG_r2scan_regen` | g-xTB | 2.46 | 1.87 |
   | 自洽 `dG_r2scan_regen` | **B97-3c** | 4.98 | **0.65** |
   | 存量标签 `dG_orca_stored` | g-xTB | 5.04 | 2.67 |
   | 存量标签 `dG_orca_stored` | **B97-3c** | 3.09 | **1.70** |

   **B97-3c 基线 single-XGB 仅用 1532 train，对真实生产标签就到 holdout MAE 1.70**
   （champion 2.215 用 22,771 train + 260-feat ensemble + GNN blend）；对自洽标签 0.65。
   gap +1.22（自洽）/ +0.97（存量），90% CI 均远离 0。

**结论：B97-3c 便宜基线是几个月来第一个真实、端到端验证的精度杠杆。** 下一步决策 =
起不起全量 relabel 战役（第 1.2 节两档方案）。**这需要用户 go。**

全部代码/文档/结果已 push（`agent/recovery-20260902`，HEAD `c6f76d0`）。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.20j %.9P %.2t %.10M %R" | grep -vE "nhc-|_cron|int2|orcasp_INT2|mace"
/usr/hpc/bin/myquota | grep -i inode          # wstor_scratch1 现 ~21%，安全
git log --oneline -8 ; git status --short
tail -260 RUN_LOG_20260903.md
sinfo -s
```
- **在跑的本项目作业**：A/B 代理 `26456516` 收尾中（09-08 06:30 时 1980/1999 chunk，
  剩 ~20 个慢任务）。1 个 `clean_orphans`（`26397167`，janitor，健康）。NHC 别碰。
- **A/B 代理跑完后**（应已完成）：重跑 merge + retrain 拿 train=1551 定数（几乎不变）：
  ```
  /home/schen3/venv/nhc-workflow/bin/python cross_benzoin/merge_rec1_prodgeom_recheck.py \
    --chunks-glob 'data/cross_benzoin/rec1_prodgeom_recheck/chunks_intermediate/int_*.csv' \
    --out-prefix data/cross_benzoin/rec1_prodgeom_recheck/rec1_b973c_intermediate_1999
  /home/schen3/venv/nequip/bin/python cross_benzoin/rec1_b973c_ab_retrain.py \
    --merged data/cross_benzoin/rec1_prodgeom_recheck/rec1_b973c_intermediate_1999_merged.csv \
    --out data/cross_benzoin/rec1_prodgeom_recheck/ab_retrain_result.json --seeds 5
  ```
  然后 `git add` 两个产物 + commit。

### 1.2 决策点 —— 起不起全量 B97-3c relabel 战役（**需用户 go**）

完整方案见 `data/cross_benzoin/rec1_prodgeom_recheck/NEXT_STEPS.md`。两档：

- **Tier A（~3-5 集群日）**：只在重生成的 funnel_v3 rank-0 几何（GFN2 --opt，跳
  ohess，复用训练表里存量 xTB thermal）上跑 **B97-3c/CPCM(DMSO) SP**，对**现有标签**
  当新基线。~5-10 CPU-h/对 × 23k ≈ 150-230k CPU-h。加 `dG_b973c_kcal` 列，
  `train_scaffold_disjoint.py` 把 `BASELINE_COL` 换成它重训。**预期 holdout MAE ≈ 1.7-1.9**
  （A/B 存量标签臂）——比 2.215 净胜 ~0.3-0.5 kcal，交付 column，成本可控。
- **Tier B（~1-2 集群周）**：重生成几何 + **同几何共同重算 r2SCAN-3c AND B97-3c**
  （= 重跑标签活动 + 1 个 SP），23-35k 对，~28 CPU-h/对 ≈ 0.6-1.0M CPU-h。自洽新
  `dG_orca_kcal` + `dG_b973c_kcal`。**预期 holdout MAE ≈ 1.0-1.5**（自洽残差地板 0.65
  被真实模型误差抬高）→ **项目级突破**。附带永久修复 purge 丢失的几何债。

**推荐**：Tier B。理由——(1) A/B 自洽臂 holdout MAE 0.65 说明地板极低，即便真实
champion（ensemble+GNN、大 train、骨架不相交）严重退化也大概率 <1.5，是 2.215 以来
从未接近过的水平；(2) Tier A 的 ~1.8 增益边际，不值一个 campaign 的机时如果不顺带
修几何债；(3) Tier B 顺带把 Rec-3 方向 1（baseline_risk 杂原子子结构富集）合批。
**但 Tier B 是 1-2 周全集群占用，必须用户拍板。** 若用户要更省，Tier A 是安全的中间步。

执行 Tier B 时：worker 直接改自 `cross_benzoin/rec1_prodgeom_recheck_worker.py`
（已是"funnel_v3 + ohess + 3 SP"的完整路径，去掉 g-xTB SP、加分片/manifest/resume
即可跑全量）；pair 清单从
`cross_round10/cross_train_table_10rounds_scaffold_split_labeled_slim260.parquet`
（35,528 行，`new_scaffold_split`）。

### 1.3 建议 2 / 3（仍待，本会话未动）

- **建议 2**（homo+cross 统一）：本会话仅侦察——`homo_unify_v1_products.csv` 只有 72 列
  （无 mordred/multiwfn），Rec 2 的 260-featurize 是完整描述符 campaign（~一个 cross
  round 规模）。用户此前决定推迟启动，等建议 1 结论。**现在建议 1 结论已出（GREEN）**
  → 若起 Tier B，建议 2 可作为并行长跑（handoff 原意），但仍建议先跟用户确认再提大
  featurize 阵列。
- **建议 3**：停 ~2k AL 轮的结论不变；方向 1（baseline_risk 杂原子富集）与 Tier B 合批。

---

## 2. 在跑 / 相关的 SLURM 作业（09-08 晨）

| jobid | 名 | 状态 | 完成后 |
|---|---|---|---|
| `26456516` | `rec1_b973c_int` | A/B 代理，1999 对，06:30 时 1980/1999，剩 ~20 慢任务 | 跑完按 §1.1 重跑 merge+retrain 定数 + commit |
| `26397167` | `clean_orphans` | ✅ janitor，`fat_rome`，健康 | 继续跑，不管 |
| `26454510` | `rec1_prodgeom` | ✅ 复核，128/128 完成 0 失败（已 merge，summary GREEN） | 完 |

**不是我的、别碰**：所有 `nhc-*` / `*_cron`、`mace-delta-train`、`orcasp_INT2_*` / `int2-*`。

**集群健康**：09-07 晚 rome/genoa/fat_rome 全 up。提交新作业前仍 `sinfo -s`。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

本会话结束时活的（都随会话失效）：
- guardian `bnhqkzl5i`（Monitor，30min）——clean_orphans 存活 2x-0 告警 + scratch1 inode
  跨 75/90/98% 告警。脚本 `scratchpad/session_guardian.sh`（scratchpad 随会话清，
  接手方重写，逻辑见 handoff 09-07 §3）。
- A/B 阵列监控 `bbmagpat8`——`26456516` 完成检测。**接手时 A/B 应已跑完**，此监控可不重建；
  只需按 §1.1 跑一次 merge+retrain。
- 复核阵列监控 `b6qqim82w`——已自然结束（128/128）。

新会话重建 guardian（Monitor 工具，内联 bash，30min）：
- **`clean_orphans` 存活**：`squeue -u schen3 -h -n clean_orphans | wc -l`，连续 2 次 0 就
  `sbatch --partition=<健康分区> pipeline/bde/clean_orphan_nodespec_workdirs.sh`（分区先
  `sinfo -h -p fat_rome -o "%a"` 判健康，不健康转 `rome`）。
- **scratch1 inode**：`myquota` 里 wstor_scratch1 的 inode %，跨 75/90/98% 告警。现 21%。
- 若起 Tier B 长跑阵列，加一条 array 完成/里程碑监控（节流：只在跨 200 任务里程碑 /
  失败数变化 / 跑完时 report，轮询 600s）。参考 `scratchpad/rec1_array_monitor.sh`
  / `rec1_intermediate_monitor.sh` 的写法。

---

## 4. 未提交 / 未 push 的 git

**干净——本会话所有产出已提交并推送。** HEAD = `c6f76d0`，`git rev-list --count
origin/agent/recovery-20260902..HEAD` = 0（A/B 跑完还有一次 merge+retrain 产物要提）。
`git status --short` 剩的都是长期存在的、非本会话的：`FILE_MAP.md` 一处、round9 模型
文件的 `M`（其他会话产出）、一堆探针/中间产物未跟踪目录（含
`rec1_prodgeom_recheck/chunks_intermediate/` 的 1999 个碎 csv —— merged/summary/
ab_retrain_result 已提交，chunks 本身不必提）。

---

## 5. 关键决策 & 不要重新讨论的事

- **champion = r1-10**（blend MAE 2.215）。不变。
- **B97-3c 便宜基线杠杆 = 真实、端到端验证**（本会话）。GREEN 复核（比值 0.33）+ A/B
  代理 POSITIVE（B97-3c 基线 Δ-model holdout MAE 1.70 vs g-xTB 2.67 对存量标签；
  0.65 vs 1.87 对自洽标签）。下一步 = 起 Tier A/B campaign，**待用户 go**。
- **归档 funnel_v3 几何不可恢复**（product 36% / aldehyde 5.6% / 三者齐全 0.6% /
  holdout 0%）。任何 B97-3c dG 都要从 SMILES 现场重生成几何。→ Tier B 顺带修几何债。
- **`repro_r2scan` std ~3.2-4.3**：新鲜 funnel_v3 搜索挑到比原始标签系统偏低 ~4-5 kcal
  的极小点（`repro_gxtb` 同几何上 median |diff| 0.04 → 几何优化没问题，差异在构象选择
  层）。→ Tier B 必须自洽 relabel，不能对旧标签加 B97-3c 列（那是 Tier A，增益打折）。
- **几何方法偏差 = 无偏差**（09-04 结案）；**round10 AL（2k）= null**；**多构象重标签 =
  null**；**3D-GNN（cross）无增益**；**homo 不比 cross 容易**。均不重议。
- **BDE champion = B6 5-seed deep ensemble，醛 1.851 / 产物 2.826**。

---

## 6. 已知陷阱

- **共享库文件跨子项目耦合**：`data/cross_benzoin/homo_v6/aldehydes_all.csv` /
  `products_all.csv` 被 BDE 和 cross-dG 同时读写。**任何改这俩 schema 的工作（Tier A/B
  会加 `dG_b973c_kcal`），改完必跑 `predict_dg.py` 20-pair smoke test**（从
  `cross_round10_dft_products.csv` 抽 20 行，`/home/schen3/venv/nequip/bin/python
  cross_benzoin/predict_dg.py --products-csv <20行> --out /tmp/...`）。memory
  `predict-dg-g-gxtb-regression-fixed`。
- **thermo_orca 循环 import**：新 worker 里 `import thermo_orca` 前必须先
  `import conf_funnel_v3`（rec1 worker 已按此写）。
- **g-xTB solvation**：用 `/home/schen3/xtb/bin/xtb --gxtb --sp --cosmo dmso`（ALPB/GBSA
  对 g-xTB 致命；COSMO ≈ CPCM）。rec1 worker 的 `_gxtb_sp` 有 cosmo→gas 回退。
- **ORCA**：`ORCA_SCF=default`（非 TightSCF，TightSCF 会 hang；<0.1 kcal 影响相对 dG）。
  `-3c` 方法不传 basis。PATH 要含 `/home/schen3/orca`。
- **临时文件写共享 GPFS**，不能用提交节点本地 `/tmp`（计算节点看不到）；worker 用
  `$TMPDIR` / `/scratch-local`。
- **`scancel` 本会话验证可用**（rc=0，之前 handoff 说被分类器拦是过时的）；仍建议 kill
  作业前说明意图。**`scontrol update`（throttle）也可用**。
- **本地大训练别在登录节点**（ensemble ~45min）——走 SLURM。A/B 的 single-XGB
  (~1500 train × 260 feat × 5 seed × 2 target) 在登录节点 ~1-2 min，可接受。
- **提交新 SLURM 作业前 `sinfo -s`**。
- 更早陷阱见 `PROJECT_SUMMARY_20260907.md` / 更早 HANDOFF。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **本文件 `HANDOFF_20260908.md`** | 会话交接；§0-1 = 建议 1 结论 + Tier A/B 决策点 |
| `HANDOFF_20260907.md` | 上一版（三条建议原文在其 §1.2），历史 |
| `data/cross_benzoin/rec1_prodgeom_recheck/NEXT_STEPS.md` | **建议 1 决策树 + 两档全量方案 + 几何可用性勘查 + 成本** |
| `data/cross_benzoin/rec1_prodgeom_recheck/rec1_prodgeom_recheck_summary.json` | 复核 GREEN 判据（128 对，分组 std / 比值 / repro 检查） |
| `data/cross_benzoin/rec1_prodgeom_recheck/ab_retrain_result.json` | A/B 代理定数（4 组 raw/Δ-model MAE + bootstrap gap + verdict） |
| `data/cross_benzoin/rec1_prodgeom_recheck/fullset_geom_coverage.txt` | 35,528 训练表的归档几何覆盖（~0%） |
| `cross_benzoin/rec1_prodgeom_recheck_worker.py` | 复核/Tier-B worker（funnel_v3+ohess+3SP，3 物种并行） |
| `cross_benzoin/select_rec1_b973c_intermediate.py` + `rec1_b973c_ab_retrain.py` + `merge_rec1_prodgeom_recheck.py` | A/B 选样 / 重训 / 参数化 merge |
| `pipeline/slurm/submit_rec1_{prodgeom_recheck,b973c_intermediate}.sh` | 复核 / A/B 的 SLURM（rome） |
| `RUN_LOG_20260903.md` | 逐步日志（living doc，本会话 09-08 段是最新） |
| `PROJECT_SUMMARY_20260907.md`（中）/ `_EN.md`（英） | 汇报底稿（未随本会话更新；建议 1 结论要补进去） |
| `cross_benzoin/CHAMPION.md` | champion 加载 / 数字 / lineage |
| `pipeline/bde/STATUS.md` | BDE 子课题权威入口 |
| memory `~/.claude/.../memory/*.md` | 见 `MEMORY.md`；本会话新增 `rec1-b973c-baseline-lever-validated` |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit -F <msg 文件>`；
   `git push`。git 卡就在第 4 节精确写清「已 staged 什么、msg 在哪、接手第一件事」。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根，同一天多次交接覆盖当天这份），用本文件
   这套小节结构：`0 一句话` / `1 立即行动`（带确切命令）/ `2 在跑的 SLURM 作业`
   （表格 + 标注别碰的）/ `3 会话内监控`（**强调随会话失效，给重建命令**）/
   `4 未提交 git` / `5 关键决策` / `6 已知陷阱` / `7 文档索引` / `8 本节（复制过去）`。
3. **`RUN_LOG_<date>.md` 收尾**：最后一行进度快照 + 任何半截状态。
4. **更新 memory**：`MEMORY.md` 里指向新 HANDOFF 的一行；非显然的新事实写成 memory 文件。
5. **产出 resume prompt**（贴给新会话），模板见下。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（全项目：cross-benzoin dG 主线 + BDE 自动化，单会话统一驱动）。中文回答。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_20260908.md，第 0-1 节是立即行动。
2. 本会话主线 = 建议 1 已 GREEN + A/B 代理 POSITIVE（B97-3c 基线 Δ-model holdout MAE
   1.70 vs champion 2.215）。决策点在 §1.2：起不起全量 B97-3c relabel 战役（Tier A ~3-5 集群日
   / Tier B ~1-2 集群周），需要我拍板——先把两档方案 + 成本 + 预期收益讲清楚让我选。
3. A/B 代理 job 26456516 若还没完，按 §1.1 跑完 merge+retrain 拿定数 + commit。
4. 会话内监控全失效——按 §3 重建 guardian（clean_orphans 存活 + inode）。
5. 提交任何新 SLURM 作业前先 sinfo -s。
6. 并行的 NHC 窗口(schen3-bc/schen3-7a)、mace-delta-train、orcasp_INT2_* 不要碰。
7. 改 aldehydes_all.csv / products_all.csv schema 后必跑 predict_dg.py 的 20-pair smoke test。
8. 进展记进 RUN_LOG_20260903.md（或当天新建）；会话结束按 §8 套路交接，记得 git push。
```
