# 交接文档 — 2026-09-08（夜间版 v3，覆盖傍晚版）

> 写给下一个接手的会话。**先读第 0-1 节。** 本文件结构固定，见第 8 节。
>
> **本会话（09-08 夜）新增**：GNN seed-ensemble 收尾 + Rec-2（homo+cross 统一）准备阶段
> 启动 + 全 220k homo 库补全盘点。用户批准：cross Tier B 之后紧接「homo 全库自洽
> B97-3c relabel」大战役。
>
> **自主推进权限（惯例，每次交接都带）**：接手会话对本文件 §1 + §5 已确立的计划有
> **自主推进权**——已知的下一步直接做（起/并/续作业、按 §1.3 merge+重训、推进 Rec-2
> 准备、写进度、git push），**无需逐步征询**。只有遇到**真正的新分叉**或**大额不可逆
> 计算**（>~50k CPU-h 的新战役、删数据、对外发布）才停下来问。NHC / kinetics /
> mace / orcasp / qm-benzoin 相关一律不碰。

---

## 0. 一句话

**champion 仍是 r1-10（blend MAE 2.215，未变）。** 本会话进展：

1. **GNN seed-ensemble 收尾**（job 26467945 seeds1-4 + blend 26472943）：4-seed GNN
   leg + w≈0.75 → blend holdout MAE **2.137**（−0.078 vs 2.215）；单 seed 方差 ±0.06。
   一次性（旧标签训），价值 = seed 方差数 + 验证 4-seed/w0.75 配方，Tier B 后按此重训 GNN。
2. **Tier B 战役进行中**：~1,800/35,528 对，0 失败，113/4441 shard，3 臂满载（~290 并发），
   ETA ~5 天。
3. **Rec-2 准备阶段启动**（用户同意「Tier B 并行准备，暂不重训」）：
   - 盘点：homo 可用标签仅 **30k**（`homo_unify_v1`）；全库其他 CSV（醛/产物 QM、BDE、
     醛 mordred）都在；**产物 mordred 在 home 备份**（`mordred_products.tar.gz` 2.4G，
     覆盖全部 53 个 champion 特征）→ 恢复中 job `26476123`（genoa，非重算）。
   - **DFT 标签全 219k：物理丢失**，不在任何 home 备份，只能重算。
4. **用户批准**：cross Tier B 落地验证后 → 起「homo 全库自洽 B97-3c relabel」战役
   （~400k SP，~2M CPU-h，3-4 周，同 `rec1_b973c_tierB_worker.py` 机制）。**不是现在起。**

全部代码/文档已 push（`agent/recovery-20260902`）。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.16j %.10P %.2t %.10M %R" | grep -vE "nhc-|_cron|int2|orcasp|qm-benzoin|mace|kinetics|wb97mv"
/usr/hpc/bin/myquota | grep -i inode
git log --oneline -6 ; git status --short
tail -200 RUN_LOG_20260903.md
sinfo -s
# Tier B 进度：
D=data/cross_benzoin/rec1_b973c_tierB/shards
awk -F, 'FNR>1{t++}FNR>1&&$6==""{e++}END{print t+0" pairs, "e+0" fail"}' $D/shard_*.csv
ls $D/*.done 2>/dev/null | wc -l   # /4441
```

### 1.2 Tier B 战役监控（主线，~5 天）

3 臂：`26467946` rome(0-1799,128-wide QOS 顶) / `26467948` fat_genoa(3120-4440) /
`26468648` fat_genoa(1800-3119)。worker `rec1_b973c_tierB_worker.py` 逐对 append+fsync
到 `shards/shard_%05d.csv`，`.done` marker + pid-skip 保证重启安全。judge 真失败 =
col6 `dG_r2scan_kcal` 空（不是尾字段 `\r`）。fat_rome 空出可加第 4 臂
（`sbatch --partition=fat_rome --array=<剩余段>%100 pipeline/slurm/submit_rec1_b973c_tierB.sh`）。
**genoa 只短借**；rome/fat_* 随便；QOS `MaxJobsPU=128` 是 rome/genoa 公平上限，预算不放宽。

### 1.3 Tier B drain 后（campaign monitor 会通知；自主执行）

1. **merge shards**（仿 `merge_rec1_prodgeom_recheck.py`）：`shards/shard_*.csv` → 一张表，
   键 `pid`；列 `dG_r2scan_kcal`(新自洽标签) / `dG_b973c_kcal`(新基线) / `dG_gxtb_kcal` /
   resid/repro 诊断。先跑 `docs/feature_audit_20260908.md` 的分布检查（`wbo_CC_new` 等
   重尾这次 winsorize）。
2. **重建训练表**：新标签 + `dG_b973c_kcal` 并进 `assemble_cross_training_table*` 输出；
   `RDKIT_FEATS` 已去 `n_CHO` → 自动 257 schema v2。**改 `homo_v6/aldehydes_all.csv` /
   `products_all.csv` schema 必跑 `predict_dg.py` 20-pair smoke test**。
3. **champion 重训**：`train_scaffold_disjoint.py`，`BASELINE_COL=dG_b973c_kcal`，257-feat，
   seed=42 → single-XGB + MLP+XGB ensemble holdout MAE。
4. **GNN 重训**：`submit_gnn_seed_ensemble_r10_cpu.sh` 指向新表 + 新 baseline，**4 seed**
   （本会话验证配方）；`blend_gnn_seed_ensemble_r10.py` 重扫 blend 权重（预期 w≈0.75）。
5. holdout MAE 落 ~1.0-1.5 → **项目级突破**，重写 CHAMPION.md / PROJECT_SUMMARY 数字，
   新 champion = r1-10-b973c。
6. 备份：`sbatch cross_benzoin/slurm/submit_backup_recovery_artifacts.sh`。

### 1.4 Rec-2 准备阶段（Tier B 并行，label 无关，自主推进）

计划全文：`data/cross_benzoin/homo_unify/REC2_PREP_PLAN_20260908.md`。
1. **产物 mordred 恢复**：job `26476123`（genoa，PENDING 排在 kinetics/wb97mv 后）→
   `homo_v6/products_mordred_full.parquet` + `products_mordred_descriptors.csv`。
   脚本 `cross_benzoin/rec2_restore_homo_product_mordred.py`。失败/不全 → fallback =
   `rec2_extract_homo_product_geoms.py`（解 `geom.tar.zst`）+ `add_mordred_cross_products.py`。
2. 校验醛/产物 × QM/mordred/BDE 的 id 对齐，产出 `homo_v6/LIBRARY_MANIFEST.md`
   （每文件行数/覆盖率/id 规范；注意 memory `bde-recovered-csv-float-id` 的 "2.0" 陷阱）。
3. 改 assembler（`assemble_cross_training_table_unified_v2.py` → v3 或参数化）走
   `--rounds 1..10` + slim257 + append `homo_unify_v1` 30k + `is_homo` + `sample_weight`
   （homo:cross≈1:1，权重~1.0）→ `homo_unify/cross_train_table_r1_10_homo_unified_slim257.parquet`。
   **到此不重训**（等 Tier B 的新标签/基线，见 §1.3 后合并）。

### 1.5 homo 全库从零 relabel 战役（**用户 2026-09-10 再确认 + 定型**，Tier B 之后起）

- **用户 09-10 澄清**：homo 要**从零重做全库**（不是 30k 捷径、不是往旧标签加列）；
  缺的 DFT 标签**补算**；模型用**单模 XGBoost + 单个 GNN，各自独立报**（不用 champion
  blend / seed 集成 / 迭代调参）。问答：**Tier B drain 后起**。
- **基础设施已备好（本会话，commit `21ff15a`）**：
  - worker `cross_benzoin/rec_homo_relabel_worker.py`（Tier B worker 的 homo 分叉，
    **2 物种/对** {prod, ald}，dG = G_prod − 2·G_ald，`_species` 原样复用）。
  - pair list `cross_benzoin/build_homo_relabel_pairs.py` →
    `data/cross_benzoin/homo_standalone/homo_relabel_pairs.csv`（**161,630 行** =
    2,000 QC + 159,630 补算；来自 `products_all.csv` 184,199 有效结构）。
  - array `cross_benzoin/slurm/submit_homo_relabel.sh`（CHUNK 12 → 13,470 任务，%250，
    `--array`/`--partition` 覆盖多臂）。
  - 规模 ~323k SP ≈ Tier B × 4.5，**~3 周**。
  - smoke job **26538271**（genoa，2 对）—— 全量前先看 GREEN。
- **启动命令 = `DRAIN_RUNBOOK.md` step 7**（drain 后与 cross retrain step 1-6 并行；
  只争集群，不互相阻塞）：
  ```
  sbatch --partition=rome      --array=0-6734%128    cross_benzoin/slurm/submit_homo_relabel.sh
  sbatch --partition=fat_genoa --array=6735-13469%150 cross_benzoin/slurm/submit_homo_relabel.sh
  ```
  起前 `sinfo -s` + 建 campaign monitor（`homo_standalone/relabel/shards/*.done` /13470）
  + 更新本 handoff。
- Phase 2/3（merge `merge_homo_relabel.py`(待写) + assemble 全库表 + 单 XGB + 单 GNN）
  = 该战役 drain 之后（~3 周），全文见 `homo_standalone/CAMPAIGN_PLAN.md`。
- **这是唯一预授权的大战役**。全 220k homo 统一模型（Rec-2）也依赖它的标签产出。

---

## 2. 在跑 / 相关的 SLURM 作业（09-08 夜）

| jobid | 名 | 状态 | 完成后 |
|---|---|---|---|
| `26467946` | tierB_rm | Tier B rome 0-1799，128R | → §1.3 |
| `26467948` | tierB_fg | Tier B fat_genoa 3120-4440，100R | → §1.3 |
| `26468648` | tierB_fg2 | Tier B fat_genoa 1800-3119，62R | → §1.3 |
| `26476123` | rec2_restore_pmo | 产物 mordred 恢复，genoa，PD(Priority) | → §1.4 校验 + manifest |
| `26464656` | clean_orphans | janitor，fat_rome，6h+ 健康 | guardian 会重提 |

**已结束（本会话）**：`26467945` GNN seeds1-4 ✅ / `26472943` blend eval ✅（genoa 10min）/
`26470870` blend eval（取消，改 genoa=26472943）/ `26475888` Rec-2 30k 重算（取消 PENDING，
被 backup 恢复取代）。

**不是我的、别碰**：`nhc-*` / `*_cron` / `mace-delta-train` / `orcasp_INT2_*` /
`qm-benzoin-*` / **`sourceB-kinetics` `recalA-wb97mv`（09-08 新出现在 genoa，用户其他工作）**。

**集群健康（09-08 夜）**：rome 大量 idle；fat_rome/fat_genoa 满（Tier B + 别人）；
genoa 有余量但别长期占。提交前 `sinfo -s`。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

- **guardian `blds3xi17`**（Monitor persistent 1800s）——clean_orphans 连续 2×0 →
  提示重提；scratch1 inode 跨 75/90/98% 告警。脚本
  `scratchpad/session_guardian.sh`（scratchpad 随会话清，接手方重写）。
- **campaign monitor `bbop7f2mv`**（Monitor persistent 600s）——Tier B 3 臂
  （`26467946 26467948 26468648`）：每 +200 shard `.done` / col6 空的真失败数上升 /
  3 臂全 drain 时 report（→ 触发 §1.3）。脚本 `scratchpad/tierB_campaign_monitor.sh`。

新会话重建（Monitor 工具，内联 bash）：
```bash
# guardian（1800s persistent）
n=$(squeue -u schen3 -h -n clean_orphans | wc -l)   # 连续 2 次 0 → sbatch --partition=<健康 fat_rome|rome> pipeline/bde/clean_orphan_nodespec_workdirs.sh
pct=$(/usr/hpc/bin/myquota | awk '/Inodes/&&/%/{for(i=1;i<=NF;i++)if($i~/%$/){gsub(/%/,"",$i);print int($i);exit}}')  # 跨 75/90/98
# campaign（600s persistent）
D=data/cross_benzoin/rec1_b973c_tierB/shards
ndone=$(ls $D/*.done 2>/dev/null|wc -l)             # 每 +200 report
nerr=$(awk -F, 'FNR>1&&$6==""{c++}END{print c+0}' $D/shard_*.csv)   # 上升即告警
inq=$(for j in 26467946 26467948 26468648; do squeue -u schen3 -h -j $j; done | wc -l)  # 0 → drain, §1.3
```
（若 `26476123` 还在跑，也顺手 watch 它离队后校验产物 mordred 输出。）

---

## 4. 未提交 / 未 push 的 git

**干净——本会话所有产出已提交并推送**（HEAD = 本次交接 commit）。
`git status --short` 剩的都是长期非本会话项：`FILE_MAP.md`、round9 模型文件 `M`（其他
会话产出）、一堆 gitignore 的 DFT/中间产物未跟踪目录（含 `rec1_b973c_tierB/shards/`、
`homo_unify/product_geoms/` —— 靠 home 备份，不进 git）。

---

## 5. 关键决策 & 不要重新讨论的事

- **champion = r1-10**（blend MAE 2.215）。不变，直到 Tier B 重训出更好的数。
- **Tier B 已起（用户 go），不重议。** 自洽 relabel（重生成几何 + 共算 r2SCAN AND
  B97-3c），不是对旧标签加列。
- **A/B 代理 POSITIVE**：B97-3c 基线 Δ-model holdout MAE 0.652（自洽）/ 1.694（存量）
  vs g-xTB 1.88 / 2.665。ratio 0.34，GREEN。
- **GNN seed-ensemble 配方定**：4 seed + w≈0.75 → blend 2.137。Tier B 后 GNN leg 按此
  重训（不是 1 seed）。单 seed 方差 ±0.06，shipped v1 恰是最差 seed。
- **特征 schema v2 = 257**：`n_CHO`×3 是 260 里唯一退化的；A/B 删除无害。
  `RDKIT_FEATS` 已改；`feature_list_257_no_nCHO_v2.json` 暂存；schema v2 在 Tier B 后重训生效。
- **Rec-2 产物 mordred = 从 home 备份恢复，不重算**（`mordred_products.tar.gz` 全量覆盖
  53 个 champion `product_mordred_*`，0 缺）。
- **Rec-2 全库 DFT 标签 = 物理丢失**（不在任何 home 备份），只有 30k 幸存。
- **用户已批准 homo 全库自洽 B97-3c relabel 战役**（§1.5）：cross Tier B 之后起，
  ~2M CPU-h / 3-4 周，同 worker。**这是唯一预授权的大战役。**
- **Rec-2 重训一次性对齐 post-Tier-B**（新标签 + 新基线 + 257 schema），准备阶段
  （mordred/描述符/统一表）现在做，重训等 Tier B。
- 几何方法偏差 = 无偏差；round10 AL（2k）= null；多构象重标签 = null；3D-GNN 无增益；
  homo 不比 cross 容易。均不重议。
- **BDE champion = B6 5-seed deep ensemble，醛 1.851 / 产物 2.826**。
- **交接是惯例**：每次会话结束/被打断都写 HANDOFF（覆盖当天）+ resume prompt，且
  resume prompt 明确带**自主推进权限**（见 §8 + 本文件顶部）。

---

## 6. 已知陷阱

- **共享库文件跨子项目耦合**：`homo_v6/aldehydes_all.csv` / `products_all.csv` 被 BDE
  和 cross-dG 同读写。**改 schema 必跑 `predict_dg.py` 20-pair smoke test**
  （`/home/schen3/venv/nequip/bin/python cross_benzoin/predict_dg.py --products-csv
  <20行 from cross_round10/cross_round10_dft_products.csv> --out /tmp/...`）。memory
  `predict-dg-g-gxtb-regression-fixed`。
- **`csv.DictWriter` 默认 `\r\n`**：Tier B worker 已 `lineterminator="\n"`；早期 shard
  （26463424 期）是 `\r\n`——判真失败用 col6 空，不是尾字段。
- **恢复 CSV 的 "2.0" 式 float id**：string-merge 到 "2"-id 库会 join 到 ~0 行。用
  `qc.norm_id`（commit 826c0e9）。memory `bde-recovered-csv-float-id`。
- **thermo_orca ⟲ conf_funnel 循环 import**：worker 里先 `import conf_funnel_v3` 再
  `import thermo_orca`。
- **g-xTB solvation**：`--gxtb --sp --cosmo dmso`（ALPB/GBSA 对 g-xTB 致命）；有 gas 回退。
- **ORCA**：`ORCA_SCF=default`（TightSCF 会 hang）；`-3c` 方法不传 basis；PATH 含
  `/home/schen3/orca`。
- **多分区 `--partition=a,b` 对 shared job 不允许**；一分区一个 sbatch。
- **QOS `MaxJobsPU=128`**：rome/genoa 有；fat_rome/fat_genoa 无。
- **临时文件用 `/scratch-local` / `$TMPDIR`**，不用提交节点 `/tmp`。
- **本地大训练/大 concat 别在登录节点**（blend eval 09-08 在登录节点被 SIGTERM；走 SLURM）。
- **fat 节点贵 50%**（`TRESBillingWeights cpu=1.5`）——不缺内存别用。
- **提交新 SLURM 作业前 `sinfo -s`**。
- 更早陷阱见 `PROJECT_SUMMARY_20260907.md` / 更早 HANDOFF。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **本文件 `HANDOFF_20260908.md`（夜间 v3）** | §0-1 = Tier B 状态 + drain 后步骤 + Rec-2 准备 + homo relabel 战役 |
| `data/cross_benzoin/homo_unify/REC2_PREP_PLAN_20260908.md` | Rec-2 全库补全盘点 + 准备步骤 + homo relabel 成本 + 219k 恢复 |
| `data/cross_benzoin/rec1_prodgeom_recheck/NEXT_STEPS.md` | Rec-1 决策树 + Tier A/B 方案 + 成本 |
| `data/cross_benzoin/rec1_prodgeom_recheck/ab_retrain_result.json` | A/B 代理定数 |
| `cross_benzoin/rec1_b973c_tierB_worker.py` | Tier B worker（homo relabel 也复用） |
| `pipeline/slurm/submit_rec1_b973c_tierB.sh` | Tier B array（CLI override 分区/range） |
| `docs/feature_audit_20260908.{md,csv}` | 260 特征审计（n_CHO 判决 + 重尾清单） |
| `data/cross_benzoin/cross_round10/gnn_seed_ensemble_r10_result.json` | GNN seed-ensemble 5-seed 结果 |
| `cross_benzoin/blend_gnn_seed_ensemble_r10.py` + `slurm/submit_blend_gnn_seed_ensemble_r10.sh` | GNN seed blend eval |
| `cross_benzoin/rec2_restore_homo_product_mordred.py` + `rec2_extract_homo_product_geoms.py` | Rec-2 产物 mordred 恢复（备份 / 归档几何两条路） |
| `RUN_LOG_20260903.md` | 逐步日志（living doc，09-08 段最新） |
| `PROJECT_SUMMARY_20260907.md`（中）/ `_EN.md`（英） | 汇报底稿（Tier B / Rec-2 落地后补） |
| `cross_benzoin/CHAMPION.md` | champion 加载 / 数字 / lineage |
| memory `~/.claude/.../memory/*.md` | 见 `MEMORY.md` |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序（这是惯例，每次都做）：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit -F <msg>`；
   `git push`。git 卡就在第 4 节精确写清「已 staged 什么、msg 在哪、接手第一件事」。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根，同一天多次交接**覆盖**当天这份），用本文件
   的小节结构：`0 一句话` / `1 立即行动`（带确切命令）/ `2 SLURM 作业`（表格 + 标注
   别碰的）/ `3 会话内监控`（强调随会话失效 + 重建命令）/ `4 未提交 git` / `5 关键
   决策` / `6 陷阱` / `7 文档索引` / `8 本节`。**文件顶部写明「自主推进权限」段落。**
3. **`RUN_LOG_<date>.md` 收尾**：最后一行进度快照 + 半截状态。
4. **更新 memory**：`MEMORY.md` 指向新 HANDOFF 的一行；非显然的新事实写成 memory 文件。
5. **产出 resume prompt**（贴给新会话），模板见下——**必须含自主推进权限条款**。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（全项目：cross-benzoin dG 主线 + BDE 自动化，单会话统一驱动）。中文回答。

0. 你对本 handoff §1 + §5 已确立的计划有【自主推进权限】：已知的下一步直接做（起/并/续
   SLURM 作业、按 §1.3 merge+重训、推进 Rec-2 准备、写 RUN_LOG、git push），无需逐步征询。
   只有遇到真正的新分叉、或 >~50k CPU-h 的新战役 / 删数据 / 对外发布 才停下来问。
   NHC(schen3-bc/7a)、nhc-*、mace-delta-train、orcasp_INT2_*、qm-benzoin-*、
   sourceB-kinetics、recalA-wb97mv 一律不碰。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_20260908.md，§0-1 是立即行动。
2. 主线 = 监控 cross Tier B 全量自洽 B97-3c relabel 战役（3 臂 26467946/26467948/26468648，
   ~5 天）。campaign monitor drain 时按 §1.3 做 merge → 训练表重建（schema v2=257）→
   champion + GNN（4-seed/w0.75）重训（BASELINE_COL=dG_b973c_kcal）。
3. 会话内监控全失效——按 §3 重建 guardian + campaign monitor。
4. Rec-2 准备阶段（Tier B 并行，不重训）：核对 job 26476123 产物 mordred 恢复 → 校验 id
   对齐 + 写 LIBRARY_MANIFEST.md → 改 assembler 出 homo+cross 统一表（§1.4）。
5. Tier B 落地验证 OK 后 → 起【已批准的】homo 全库自洽 B97-3c relabel 战役（§1.5，
   ~2M CPU-h / 3-4 周，同 rec1_b973c_tierB_worker.py）。起前 sinfo -s + 建 monitor。
6. 提交新 SLURM 前 sinfo -s。genoa 只短借；rome/fat_* 随便；QOS MaxJobsPU=128 不放宽。
   fat 节点贵 50%，不缺内存别用。
7. 改 aldehydes_all.csv / products_all.csv schema 后必跑 predict_dg.py 20-pair smoke test。
8. 进展记进 RUN_LOG_20260903.md；会话结束按 §8 套路交接（惯例），resume prompt 带自主
   推进权限，记得 git push。
```
