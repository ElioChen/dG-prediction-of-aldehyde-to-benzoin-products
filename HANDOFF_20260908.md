# 交接文档 — 2026-09-08（傍晚版，Tier B 战役进行中）

> 写给下一个接手的会话。**先读第 0-1 节。** 本会话（09-08 白天）在用户 go 下起了
> **Rec-1 Tier B 全量自洽 B97-3c relabel 战役**，并做了一批等待期并行工作。只有 NHC
> （`schen3-bc`/`schen3-7a`、`nhc-*`、`nhc_backup_cron`）、`mace-delta-train`、
> `orcasp_INT2_*` / `qm-benzoin-*` 不碰。本文档结构固定，见第 8 节。
>
> 本文件覆盖了早晨版（早晨版内容已并入 RUN_LOG + 下面第 5 节）。

---

## 0. 一句话

**champion 仍是 r1-10（blend MAE 2.215，未变）。** 本会话：

1. **Rec-1 A/B 代理收尾**（job 26456516，1999/1999）→ 定数：B97-3c 基线 Δ-model
   holdout MAE **0.652**（自洽标签）/ **1.694**（存量标签）vs g-xTB 1.88 / 2.665。
   A/B **POSITIVE 确认**。
2. **Tier B 全量战役已起**（用户 go = 上一个 resume prompt 第 9 条「执行 Tier B」）：
   35,528 对全部从 SMILES 重生成 funnel_v3+ohess 几何，同几何共同重算 r2SCAN-3c +
   B97-3c CPCM(DMSO) SP → 新自洽 `dG_r2scan` 标签 + 新 `dG_b973c_kcal` 基线。pilot
   （26462430）已端到端验证 worker。**预期 champion holdout MAE ~1.0-1.5**（A/B 自洽臂
   地板 0.65、存量臂 1.70）vs 现 2.215。
3. **加速**：rome QOS `MaxJobsPU=128` 卡住单臂 → 拆成 **3 臂**（rome + 2×fat_genoa），
   当前 running 210，ETA 从 ~10 天压到 **~5-6 天**。
4. **等待期并行**：特征 schema 审计（`n_CHO`×3 是唯一退化特征，已 stage 257-list）；
   GNN seed-ensemble 脚手架（等 4 个 seed）；备份脚本加 Tier B stanza；guardian 重建。

全部代码/文档已 push（`agent/recovery-20260902`，HEAD `4a9ab7d`）。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.14j %.10P %.2t %.10M %R" | grep -vE "nhc-|_cron|int2|orcasp|qm-benzoin|mace"
/usr/hpc/bin/myquota | grep -i inode          # wstor_scratch1 现 ~21%，安全
git log --oneline -6 ; git status --short
tail -180 RUN_LOG_20260903.md
sinfo -s
# Tier B 进度：
D=data/cross_benzoin/rec1_b973c_tierB/shards
awk -F, 'FNR>1{t++}FNR>1&&$6==""{e++}END{print t+0" pairs, "e+0" fail"}' $D/shard_*.csv
ls $D/*.done 2>/dev/null | wc -l   # 完成的 shard（每 shard=8 对）
```

### 1.2 Tier B 战役监控（主线，~5-6 天）

**3 个 array 臂，disjoint shard 段，`submit_rec1_b973c_tierB.sh` + CLI override：**

| jobid | 分区 | array (shard 段) | 备注 |
|---|---|---|---|
| `26467946` | rome | `0-1799` | 128-wide（rome QOS 硬上限） |
| `26467948` | fat_genoa | `3120-4440%150` | 82 running |
| `26468648` | fat_genoa | `1800-3119%150` | 排队，随 26467948 shard 完成填入 |

- worker `cross_benzoin/rec1_b973c_tierB_worker.py`：每 task 8 对（CHUNK=8），逐对
  append+fsync 到 `shards/shard_%05d.csv`，重启按 pid 跳过。**任何臂重叠/重启都安全**
  （`.done` marker + 逐对 pid-skip）。
- **fat_rome 当前物理占满**（9152/9216 CPU，别人占的，非预留）。一旦 drain，可加
  第 4 臂：`sbatch --partition=fat_rome --array=<剩余段>%100 --job-name=tierB_fr4
  pipeline/slurm/submit_rec1_b973c_tierB.sh`（先 `sinfo -h -p fat_rome -o "%C"` 看 Idle）。
- **`genoa`（regular）不要长期占**（用户：他项目预留，只可短借）。`rome`/`fat_*` 随便。
- QOS `MaxJobsPU=128` 是 rome/genoa 分区 QOS 的公平上限，**预算充足也不放宽**；
  fat_rome/fat_genoa QOS 无此上限（TRES cap 9216 CPU/池）。

### 1.3 Tier B drain 后（campaign monitor 会通知）

1. **merge shards**：写个 merge（仿 `merge_rec1_prodgeom_recheck.py`）把
   `shards/shard_*.csv` 拼成一张表，键 = `pid`。列：`dG_r2scan_kcal`（新自洽标签）、
   `dG_b973c_kcal`（新基线）、`dG_gxtb_kcal`（同几何 g-xTB）、resid/repro 诊断。
   先跑 `docs/feature_audit_20260908.md` 里的分布检查（`wbo_CC_new` 等重尾这次要
   winsorize）。
2. **重建训练表**：把 `dG_b973c_kcal` + 自洽 `dG_r2scan` 并进
   `assemble_cross_training_table_*` 的输出（`assemble_cross_training_table.py` 的
   `RDKIT_FEATS` 本会话已去掉 `n_CHO` → 自动 257 特征 schema v2）。
   **改 `homo_v6/aldehydes_all.csv` / `products_all.csv` schema 的话必跑
   `predict_dg.py` 20-pair smoke test**（memory `predict-dg-g-gxtb-regression-fixed`）。
3. **champion 重训**：`train_scaffold_disjoint.py`，`BASELINE_COL=dG_b973c_kcal`
   （在 `train_cross_delta.py:47`，或加 `--baseline-col`），257-feat，seed=42。
   出 single-XGB + MLP+XGB ensemble holdout MAE。
4. **GNN 重训**：`submit_gnn_seed_ensemble_r10_cpu.sh` 改指向新表 + 新 baseline，
   多 seed；`blend_gnn_seed_ensemble_r10.py` 重扫 blend 权重。
5. 若 holdout MAE 落到 ~1.0-1.5 → **项目级突破**，重写 CHAMPION.md / PROJECT_SUMMARY
   所有数字，新 champion = r1-10-b973c。
6. 备份：`sbatch cross_benzoin/slurm/submit_backup_recovery_artifacts.sh`（已加
   `rec1_b973c_tierB_shards` stanza；drain 后 + merge 后各跑一次）。

### 1.4 GNN seed-ensemble（次要，等 4 个 seed）

`26467945`（fat_rome，seeds 1-4，卡队列中）跑完 → `/home/schen3/venv/nequip/bin/python
cross_benzoin/blend_gnn_seed_ensemble_r10.py` → 看累积 seed 平均 GNN leg 能否把 blend
压过 2.215。⚠ 这批用**当前 r10 标签**训，对 post-Tier-B champion 一次性；价值 = seed
方差数 + 验证 seed-ensemble 配方。watch task `bopacsdl1`（bg）。

### 1.5 建议 2（homo+cross 统一）—— 仍待用户确认

Task C 代理正（1:1 比例 naive_merge +0.11 kcal）。前置 = 30k `homo_unify_v1_products.csv`
（现 72 列）做完整 260/257-schema featurize（~1-2 天阵列）。**起大 featurize 前先跟
用户确认。** handoff 原意：可作 Tier B 的并行长跑。

---

## 2. 在跑 / 相关的 SLURM 作业（09-08 傍晚）

| jobid | 名 | 状态 | 完成后 |
|---|---|---|---|
| `26467946` | tierB_rm | Tier B rome 臂 shard 0-1799，128 running | → §1.3 merge+retrain |
| `26467948` | tierB_fg | Tier B fat_genoa 臂 shard 3120-4440，82 running | 同上 |
| `26468648` | tierB_fg2 | Tier B fat_genoa 臂 shard 1800-3119，排队 | 同上 |
| `26467945` | gnn_seed_r10f | GNN seeds 1-4，fat_rome，卡队列 | → §1.4 blend 分析 |
| `26464656` | clean_orphans | janitor，fat_rome，健康（guardian 09-08 自动重提的） | 3 天后再 TIMEOUT，guardian 会再补 |

**已结束（本会话）**：`26456516` A/B 代理 1999/1999 ✅；`26462430` Tier B pilot 4/4 ✅；
`26463424` Tier B 单臂（取消，被 3-way 取代，进度经 pid-skip 无损）；
`26464493`/`26467488`/`26467578` GNN seed 早期版（全取消，见第 5 节）。

**不是我的、别碰**：所有 `nhc-*` / `nhc_backup_cron` / `*_cron`、`mace-delta-train`、
`orcasp_INT2_*` / `int2-*`、`qm-benzoin-*`。

**集群健康（09-08 傍晚）**：rome 大量 idle；fat_rome 物理占满（别人）；fat_genoa 我占
82；genoa 有余量但**别长期占**。提交新作业前仍 `sinfo -s`。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

本会话结束时活的（都随会话失效）：
- **guardian `bhb5aij9l`**（Monitor，persistent，1800s）——clean_orphans 存活 2x-0 →
  自动 `sbatch` 重提（fat_rome，不健康转 rome）；scratch1 inode 跨 75/90/98% 告警。
  脚本 `scratchpad/session_guardian.sh`（scratchpad 随会话清，接手方重写）。
  **本会话已实际触发一次**（26397167 TIMEOUT → 自动重提 26464656），逻辑验证有效。
- **campaign monitor `bje4x4dx1`**（Monitor，persistent，600s）——Tier B 3 臂
  （`26467946 26467948 26468648`）：每 +200 shard `.done` / 真失败数上升（col 6
  `dG_r2scan_kcal` 空）/ 3 臂全 drain 时 report。脚本
  `scratchpad/tierB_campaign_monitor.sh`。
- **GNN seed watch `bopacsdl1`**（Bash bg，until-loop）——`26467945` 离队即 report
  seed checkpoint 数。

新会话重建（Monitor 工具，内联 bash）：
```bash
# guardian（30min，persistent）
n=$(squeue -u schen3 -h -n clean_orphans | wc -l)  # 连续 2 次 0 就
  sbatch --partition=<sinfo 判健康的 fat_rome 或 rome> pipeline/bde/clean_orphan_nodespec_workdirs.sh
pct=$(/usr/hpc/bin/myquota | awk '/wstor_scratch1/{f=1} f&&/Inodes/&&/%/{for(i=1;i<=NF;i++)if($i~/%$/){gsub(/%/,"",$i);print int($i);exit}}')  # 跨 75/90/98 告警
# campaign（600s，persistent）
D=data/cross_benzoin/rec1_b973c_tierB/shards
ndone=$(ls $D/*.done 2>/dev/null|wc -l)            # 每 +200 report
nerr=$(awk -F, 'FNR>1&&$6==""{c++}END{print c+0}' $D/shard_*.csv)  # 上升即告警
inq=$(for j in 26467946 26467948 26468648; do squeue -u schen3 -h -j $j; done | wc -l)  # 0 → drain, 触发 §1.3
```

---

## 4. 未提交 / 未 push 的 git

**干净——本会话所有产出已提交并推送。** HEAD = `4a9ab7d`，
`git rev-list --count origin/agent/recovery-20260902..HEAD` = 0。
`git status --short` 剩的都是长期存在的非本会话项：`FILE_MAP.md` 一处、round9 模型
文件 `M`（其他会话产出）、一堆探针/中间产物未跟踪目录（含
`rec1_b973c_tierB/shards/` 的碎 csv —— 是 gitignore 的 DFT 计算，靠 home 备份
`submit_backup_recovery_artifacts.sh` 的 `rec1_b973c_tierB_shards` stanza，不进 git）。

---

## 5. 关键决策 & 不要重新讨论的事

- **champion = r1-10**（blend MAE 2.215）。不变，直到 Tier B 重训出更好的数。
- **Tier B 已起（用户 go），不重议要不要起。** 自洽 relabel（重生成几何 + 共算
  r2SCAN AND B97-3c），不是对旧标签加 B97-3c 列（那是被否掉的 Tier A）。
- **A/B 代理 POSITIVE**：B97-3c 基线 Δ-model holdout MAE 0.652（自洽）/ 1.694（存量）
  vs g-xTB 1.88 / 2.665。std 比值 0.34，GREEN。
- **保留同几何 g-xTB SP**（偏离早晨 handoff「去掉」）：cost <5%，换全尺度自洽 A/B 基线。
- **特征 schema v2 = 257**：`n_CHO`×3 是 260 里唯一退化的（top value >99%，XGBoost
  gain-importance 恰为 0）；A/B 验证删除无害（ΔMAE 0.006-0.02，seed 噪声内）。
  `assemble_cross_training_table.py` 的 `RDKIT_FEATS` 已去 `n_CHO`；
  `data/cross_benzoin/feature_list_257_no_nCHO_v2.json` 暂存；**shipped r1-10 冠军不动**
  （加载自己冻结的 260 列），schema v2 在 Tier B 后重训生效。
  重尾特征（`wbo_CC_new` max 1.63 等）保留，Tier B 几何重生成后 winsorize。
- **GNN seed 阵列迁移史**：GPU（26464493，队列满）→ rome CPU（26467488，抢 Tier B
  QOS）→ genoa（26467578，他项目预留）→ **fat_rome（26467945，当前）**。
- **恢复基本完成**：round2-10 DFT-SP 标签全在位，homo_v6 库五件套行数正常。唯一
  未闭合缺口 = `homo_unify_v1_products.csv` 只有 72 列（Rec-2 前置 featurize 未起）。
- 几何方法偏差 = 无偏差（09-04 结案）；round10 AL（2k）= null；多构象重标签 = null；
  3D-GNN（cross）无增益；homo 不比 cross 容易。均不重议。
- **BDE champion = B6 5-seed deep ensemble，醛 1.851 / 产物 2.826**。

---

## 6. 已知陷阱

- **共享库文件跨子项目耦合**：`data/cross_benzoin/homo_v6/aldehydes_all.csv` /
  `products_all.csv` 被 BDE 和 cross-dG 同时读写。**任何改这俩 schema 的工作，改完
  必跑 `predict_dg.py` 20-pair smoke test**（`/home/schen3/venv/nequip/bin/python
  cross_benzoin/predict_dg.py --products-csv <20行> --out /tmp/...`，20 行从
  `cross_round10/cross_round10_dft_products.csv` 抽）。memory
  `predict-dg-g-gxtb-regression-fixed`。
- **`csv.DictWriter` 默认行尾 `\r\n`**：Tier B worker 已改 `lineterminator="\n"`，
  但早期 shard（26463424 时期）是 `\r\n`——pandas 可读，但朴素逗号切分会把尾部 `\r`
  当字段。判真失败用「`dG_r2scan_kcal`（col 6）为空」，不是尾字段。
- **thermo_orca 循环 import**：worker 里 `import thermo_orca` 前必须先
  `import conf_funnel_v3`（Tier B worker 已按此写）。
- **g-xTB solvation**：`/home/schen3/xtb/bin/xtb --gxtb --sp --cosmo dmso`（ALPB/GBSA
  对 g-xTB 致命）。worker 的 `_gxtb_sp` 有 cosmo→gas 回退。
- **ORCA**：`ORCA_SCF=default`（非 TightSCF，会 hang）。`-3c` 方法不传 basis。
  PATH 含 `/home/schen3/orca`。
- **多分区 `--partition=a,b` 对 shared job 不允许**（"combination not supported"）；
  一分区一个 sbatch。
- **QOS `MaxJobsPU=128`**：rome/genoa 分区 QOS 有；fat_rome/fat_genoa 无。预算不影响。
- **临时文件写共享 GPFS**，worker 用 `/scratch-local` / `$TMPDIR`，不用提交节点 `/tmp`。
- **`scancel` / `scontrol update`（throttle）本会话验证可用**（rc=0）。
- **本地大训练别在登录节点**（MLP ensemble 5-seed 在登录节点被 SIGTERM）——走 SLURM。
- **提交新 SLURM 作业前 `sinfo -s`**。
- 更早陷阱见 `PROJECT_SUMMARY_20260907.md` / 更早 HANDOFF。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **本文件 `HANDOFF_20260908.md`（傍晚版）** | 会话交接；§0-1 = Tier B 战役状态 + drain 后步骤 |
| `HANDOFF_20260907.md` | 三条建议原文（其 §1.2），历史 |
| `data/cross_benzoin/rec1_prodgeom_recheck/NEXT_STEPS.md` | Rec-1 决策树 + Tier A/B 方案 + 几何勘查 + 成本 |
| `data/cross_benzoin/rec1_prodgeom_recheck/ab_retrain_result.json` | A/B 代理定数（train=1551，4 组 MAE + gap + verdict） |
| `cross_benzoin/rec1_b973c_tierB_worker.py` | Tier B worker（funnel_v3+ohess + 3 SP，逐对 append/resume） |
| `cross_benzoin/select_rec1_b973c_tierB.py` | 全 35,528 对清单生成（test+val 在前） |
| `pipeline/slurm/submit_rec1_b973c_tierB.sh` | Tier B array（CLI override 分区/range 复用） |
| `data/cross_benzoin/rec1_b973c_tierB/rec1_b973c_tierB_pairs_35528.csv` | pair 清单 |
| `docs/feature_audit_20260908.{md,csv}` | 260 特征分布审计（n_CHO 判决 + 重尾清单 + A/B） |
| `data/cross_benzoin/feature_list_257_no_nCHO_v2.json` | schema v2（257）暂存 |
| `cross_benzoin/blend_gnn_seed_ensemble_r10.py` + `slurm/submit_gnn_seed_ensemble_r10_cpu.sh` | GNN seed-ensemble |
| `RUN_LOG_20260903.md` | 逐步日志（living doc，本会话 09-08 段最新） |
| `PROJECT_SUMMARY_20260907.md`（中）/ `_EN.md`（英） | 汇报底稿（Tier B 结论落地后要补） |
| `cross_benzoin/CHAMPION.md` | champion 加载 / 数字 / lineage |
| `ALDEHYDE_LIBRARY_AND_DG_WORKFLOW_20260907.md` | 醛库史 + dG 工作流 + 描述符工程完整清单 |
| `data/analysis/homo_cross_gap/FINDING.md` | homo/cross 分解 + Task C（联合）/ Task D（便宜基线）完整数字 |
| `pipeline/bde/STATUS.md` | BDE 子课题权威入口 |
| memory `~/.claude/.../memory/*.md` | 见 `MEMORY.md`；本会话新增 `feature-schema-v2-drop-ncho`，更新 `rec1-b973c-baseline-lever-validated` |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit -F <msg 文件>`；
   `git push`。git 卡就在第 4 节精确写清「已 staged 什么、msg 在哪、接手第一件事」。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根，同一天多次交接覆盖当天这份），用本文件
   这套小节结构：`0 一句话` / `1 立即行动`（带确切命令）/ `2 在跑的 SLURM 作业`
   （表格 + 标注别碰的）/ `3 会话内监控`（**强调随会话失效，给重建命令**）/
   `4 未提交 git` / `5 关键决策` / `6 已知陷阱` / `7 文档索引` / `8 本节（复制过去）`。
3. **`RUN_LOG_<date>.md` 收尾**：最后一行进度快照 + 任何半截状态。
4. **更新 memory**：`MEMORY.md` 里指向新 HANDOFF 的一行；非显然的新事实写成 memory 文件。
5. **产出 resume prompt**（贴给新会话），模板见下。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（全项目：cross-benzoin dG 主线 + BDE 自动化，单会话统一驱动）。中文回答。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_20260908.md，第 0-1 节是立即行动。
2. 本会话主线 = 监控 Rec-1 Tier B 全量自洽 B97-3c relabel 战役（3 臂：26467946 rome /
   26467948+26468648 fat_genoa；~5-6 天；~400+/35528 对完成，0 失败）。campaign monitor
   drain 时按 §1.3 做 merge → 训练表重建（schema v2 = 257，去 n_CHO）→ champion +
   GNN 重训（BASELINE_COL=dG_b973c_kcal）。
3. 会话内监控全失效——按 §3 重建 guardian（clean_orphans 存活 + inode）+ campaign monitor。
4. 提交任何新 SLURM 作业前先 sinfo -s。genoa 只可短借（他项目预留），rome/fat_* 随便；
   QOS MaxJobsPU=128 是 rome/genoa 分区 QOS 上限，预算不影响。
5. 并行的 NHC 窗口(schen3-bc/schen3-7a)、nhc-*、mace-delta-train、orcasp_INT2_*、
   qm-benzoin-* 不要碰。
6. 改 aldehydes_all.csv / products_all.csv schema 后必跑 predict_dg.py 的 20-pair smoke test。
7. GNN seed 阵列 26467945（fat_rome）跑完 → blend_gnn_seed_ensemble_r10.py 出 seed 方差数。
8. 进展记进 RUN_LOG_20260903.md；会话结束按 §8 套路交接，记得 git push。
```
