# 交接文档 — 2026-09-04（窗口 B：cross-benzoin dG）

> 写给下一个接手的会话。**先读第 0-1 节，再读第 3 节重建监控。**
> 并行的 NHC 窗口（schen3-bc）、BDE 自动化、`mace-delta-train`、`orcasp_INT2_*` 不要碰（第 2 节标了）。
> 本文档结构是**固定套路**，见第 8 节 —— 下次交接照着写。前一份是 `HANDOFF_20260903.md`（r8/9 恢复过程）。

---

## 0. 一句话

**r8/9 DFT 标签恢复 + r1-9/r1-10 重训 全部完成。champion = r1-10**（blend MAE 2.215，
P(blend>ensemble) 0.999，w_gnn 0.5；`cross_benzoin/CHAMPION.md` 固定了加载方式）。
`predict_dg.py` 端到端预测 wrapper 已做（assemble+predict 半段验证 MAE 1.65）。
round10 三腿 SP + 标签组装完成（1969 对），AL 消融 = **null**（训练那批 AL 点 MAE 动
−0.03，噪声内）。

**现在在做**：标签质量调查，判断"还能不能推 MAE"还是"部署 + 换问题形态"。
- `confnoise 26368077` ≈完成：构象噪声地板 std ~2.9 → proxy MAE ~2.3 ≈ blend 2.215；
  Boltzmann 只挪 0.46 kcal。**→ 模型已到单构象标签噪声天花板；多构象重标签 = null。**
- `geom_bias 26368749` 完成：GFN2 vs g-xTB 几何，产物侧 r2SCAN-3c E median −1.5 kcal，
  **结构化于超价 P / 多磺酰 / 硼**（非超价 S ≈0）。杂原子重的 ~20-30% 子类有真实几何偏差。
- `dg_geom_method 26371408` 🏃在跑（~4h，13:40 起）：**决定性测试** —— 产物侧几何偏差
  在三物种 ΔG 里被 donor/acc 抵消后还剩多少（hetero vs control，带 RMSD 控制）。

**单一最重要的下一步**：等 `26371408` 出结果（监控 `bd5o4jszp` 会报）：
- 若 hetero 组 ddG median >1 kcal 且 >> control 且 rmsd_prod_med <0.3（干净）→ 对杂原子
  重的 ~5-8k 对做定向 g-xTB 重优化几何 + r2SCAN 重标签，重训，在 B/S/P 富集 holdout 上比。
- 否则 → 几何不是杠杆 → **Goal 3**：部署 `predict_dg.py`（做批量打分服务）+ 换问题形态
  （ΔG 回归 → favorable/unfavorable 分类排序；或端到端 3D GNN，现有 35k 带 DFT 标签够训）。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.20j %.9P %.2t %.10M %R" | grep -vE "nhc-|_cron|bde_|mace|int2|orcasp_INT2"
/usr/hpc/bin/myquota | grep -A3 wstor_scratch1        # inode，>60% 才需要动手（现 ~20%）
git log --oneline -5 ; git status --short             # git 会间歇卡，见第 4 节
tail -80 RUN_LOG_20260903.md                          # 逐步日志（09-03 起，含 09-04 全部）
```

### 1.2 等 `dg_geom_method 26371408`（主线）
- 监控 `bd5o4jszp` 报 `*** DG_GEOM_METHOD DONE`，或手动：
  ```
  python3 - <<'PY'
  import glob,csv,statistics as st
  h=[];c=[];rp=[]
  for f in glob.glob('data/cross_benzoin/dg_geom_method/chunks/dgm_*.csv'):
    for r in csv.DictReader(open(f)):
      v=r.get('ddG_kcal')
      if v and abs(float(v))<1e4:
        (h if r.get('grp')=='hetero' else c).append(float(v))
        if r.get('rmsd_prod'): rp.append(float(r['rmsd_prod']))
  print(f'n={len(h)+len(c)}/60  hetero med {st.median(h):+.2f} (n{len(h)})  ctrl med {st.median(c):+.2f} (n{len(c)})  rmsd_prod med {st.median(rp):.2f}')
  PY
  ```
- **判读**：`rmsd_prod_med` 必须 <0.3（否则 g-xTB opt 又跑偏了构象，结论无效 —— 那说明
  worker 的 "g-xTB opt 从 GFN2 极小点精修" 没生效，去查 `dgm_*.out` 的 gxtb.log）。
  干净的话：hetero median ddG 是杂原子子类的真标签偏差；control median 是基线噪声。
  hetero − control 就是"换 g-xTB 几何能修掉多少"。

### 1.3 根据结果分叉
- **有偏差（hetero ddG >1 & >> ctrl）**：
  1. 从 r1-10 slim260 表选杂原子重的行（`smiles` 含 `P(=O)` / `S(=O)(=O)` / 硼酯），
     ~5-8k 对。
  2. 对这些对的三物种：g-xTB 从 GFN2 几何精修 → r2SCAN-3c SP → 新 `dG_orca_kcal`。
     复用 `cross_benzoin/dg_geom_method_worker.py` 的 `_opt`/`_sp`（去掉 fresh embed，
     直接读产物/醛的生产 GFN2 几何 —— 醛几何在
     `~/benzoin_backups/recovery_20260902/r{10,89}_aldehyde_regen_geometry.tar.gz`，
     产物在 `cross_round10_fat20_stage1_xyz_geometry.tar.gz`）。
  3. 用新标签覆盖那些行，重跑 `run_r19_retrain_chain.sh` 的 v10 版（`cross_round10`），
     在一个 B/S/P 富集 holdout 上比 vs 现 champion。
- **无偏差** → Goal 3。两条：
  - **部署**：`predict_dg.py` 已能用（见 `CHAMPION.md`）。做个批量版：给一个候选库
    parquet，跑 `submit_predict_dg.sh` 批量 featurize → predict → 排序输出。
  - **换问题形态**：分类/排序（阈值判 favorable，鲁棒于 2 kcal 标签噪声）；或端到端 3D
    GNN（`cross_benzoin/gnn3d_*` 脚本已在 repo，35k 带 r2SCAN 标签的对够训）。

### 1.4 收尾（无论分叉）
- push 分支 `agent/recovery-20260902`（等确认 NHC/BDE 窗口也收齐 —— 见第 5 节）。
- 更新 memory：新 champion、AL null、标签天花板结论。

---

## 2. 在跑 / 相关的 SLURM 作业（09-04 ~14:00）

| jobid | 名 | 干什么 | 状态 | 完成后 |
|---|---|---|---|---|
| `26368077` | `confnoise_cross` | 构象噪声地板（K=5 conf × r2SCAN SP，32 产物） | ✅ 31/32，std 2.91 → floor 2.32 | 结论：到天花板 |
| `26368749` | `geom_bias` | 产物侧 GFN2 vs g-xTB 几何 → r2SCAN E（12 B/S/P 产物） | ✅ DONE，median −1.5，结构化 | 见 1.2 |
| **`26371408`** | **`dg_geom_method`** | **三物种 ΔΔG，GFN2 vs g-xTB 几何（60 对 = 35 hetero + 25 ctrl，带 RMSD）** | 🏃 ~4h（13:40 起，13:56 时 1 行） | **见 1.2-1.3，主线** |
| `26368779` | `wb97x_shift` | r2SCAN vs wB97X-3c 泛函偏差 | ❌ CANCELLED（wB97X-3c SP 返回垃圾能量，`_parse_orca_energy` 处理不了 -3c 输出） | 不重试 |

**不是我的、别碰**：`mace-delta-train`（fat_genoa，别的窗口在做 MACE-delta）；
`orcasp_INT2_*`（INT2 项目）；所有 `nhc-*` / `*_cron`（NHC 窗口 schen3-bc）；
BDE 自动化 `26316404`/`26324801`/`26326313`/`26348174`/`26348175`（b1 降了节流，见第 5 节）。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

用 Monitor 工具重建（persistent，timeout 3600000）：

1. **`26371408` + `26368077` 完成** → 报聚合结果。轮询两个 `squeue -j`，各自空队列时
   算 confnoise `dG_std_kcal` 均值 + dg_geom_method 的 hetero/ctrl median ddG + rmsd_prod_med。
   本会话 = `bd5o4jszp`（命令在其历史里，照抄改 jobid）。
2. **scratch1 inode 预警**（>75% 报警）：`/usr/hpc/bin/myquota | awk '/wstor_scratch1/{f=1}
   f&&/Inodes/{print $5}'`，分级 75/90/100% 报。本会话 = `bjadumout`（已随会话结束失效，
   重建：inode 09-03 夜里爆过 133%，现 20%，janitor `26348175` 常驻）。
3. 若开新的重标签 / 重训作业，加对应完成监控。

---

## 4. 未提交 / 未 push 的 git

- HEAD = `f067ba8`，分支 `agent/recovery-20260902`，**领先 origin 很多，全部未 push**
  （从 09-02 起就没 push，等三个窗口收齐统一 push）。
- git 在这个 GPFS mount 上间歇卡（`git status` 会挂 >2min）。`rm -f .git/index.lock`
  后重试，负载低时能过。`git log` 从 pack 读，快。
- 09-04 期间每步都 commit 了（HEAD 往前数 ~30 个 commit 是今天的）。工作树应该干净，
  但 `data/cross_benzoin/dg_geom_method/`、`geom_bias/`、`confnoise_cross/`、`wb97x_shift/`
  下的 chunk CSV 是探针输出（小，可 commit 或 gitignore，随意）。

---

## 5. 关键决策 & 不要重新讨论的事

- **champion = r1-10**（不是 r1-9）。r1-9 blend 2.167 vs r1-10 blend 2.215 是 n=448
  噪声内；r1-10 多 1969 对 DFT 覆盖、blend 优势更强（w_gnn 0.4→0.5，P 0.995→0.999）。
  r1-9 留作 fallback。见 `cross_benzoin/CHAMPION.md`。
- **round10 AL = null**。`eval_round10_al_benefit.py`：留 30% round10 当 hard-test，用/不用
  另 70% 训 ensemble，B−A = −0.033（噪声内）。AL *诊断*出了真盲点（r1-9 在选点上 MAE
  2.78 vs holdout 2.17，uncertainty↔error r=0.40），但*训练*那 1370 个点没改善。
  **别再提 2k 规模的 AL 轮**；要推需要大得多的批次或换杠杆。
- **多构象 Boltzmann 重标签 = null**（confnoise 的 `single_minus_boltz_kcal` mean 0.46；
  homo 上也是 2× 确认的 null，memory `homo-active-relabel-null-result`）。
- **inode 危机（09-03 夜 133%）** 根因 = BDE geomarch 积压（~600 未归档 chunk ≈ 25 万散
  xyz），b1 从 int6 窗口处理：降了 BDE 阵列节流（`26316404` 40→8，`26324801` 100→15）。
  **window A 要在 inode <90% 时调回**（`scontrol update JobId=... ArrayTaskThrottle=...`）。
- 修过的 5 个 purge 后脚本漂移 bug（都 commit 了）：GNN `--ensemble-path` 文件名、
  09-01 旧 r1-9 目录、`prune` META 缺 `new_scaffold_split`、round10 醛腿漏 r89 醛、
  `geom_bias` 共享目录 ORCA 竞态。

---

## 6. 已知陷阱

- **两个 python 环境**：`/home/schen3/venv/nequip`（torch_geometric，跑 GNN / predict_dg）
  vs `/home/schen3/venv/nhc-workflow`（mordred / rdkit / assemble / ORCA worker）。
  没有一个环境同时有两者 —— `predict_dg.py` 在 nequip 下跑、shell out 到 nhc-workflow。
- **ORCA MPI（nprocs>1）在这里跑不通** —— 返回 None。所有 ORCA SP 用 nprocs=1 串行，
  靠 worker 数并行。r2SCAN-3c SP 对 40-60 原子分子 ~25-35 min/个。
- **wB97X-3c SP**：`_parse_orca_energy` 解析不出（-3c 范围分离杂化输出格式不同），返回
  ~4e6 的垃圾。要用 wB97X-3c 得先修解析。
- **`conformer_noise_worker.py` / 各 probe worker 里硬编码过 purge 掉的
  `/scratch-shared/schen3/benzoin-dg/pipeline/compute`** —— 09-04 都改成
  `/gpfs/scratch1/shared/schen3/benzoin-dg-restored/pipeline/compute` 了，新写 worker 注意。
- **几何方法消融必须"g-xTB 从 GFN2 极小点精修"**，不能各自从新 ETKDG 种子独立优化 ——
  否则落进不同构象，ddG 变成构象噪声（v1 的 rmsd_prod 0.87 Å 就是这个）。
- **`assemble_cross_training_table_v3` 的 `load_round` 要求 `dG_orca_kcal` 标签**（inner
  join）—— 纯预测时 `predict_dg.py` 塞了个全 0 的假 dft_sp。
- git 在 GPFS 上间歇卡（见第 4 节）。
- inode 整天 20-48% 震荡；janitor `26348175` 常驻。>60% 才动手。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **本文件** | 09-04 交接（标签质量调查 + 分叉） |
| `HANDOFF_20260903.md` | 09-03 交接（r8/9 恢复过程、inode 危机、r1-9/r1-10 链） |
| `RUN_LOG_20260903.md` | 09-03+09-04 逐步日志（**living doc**，所有 jobid / 决策 / bug） |
| `cross_benzoin/CHAMPION.md` | 当前 champion（r1-10）加载方式、数字、lineage、为何 r1-10 |
| `cross_benzoin/predict_dg.py` + `slurm/submit_predict_dg.sh` | 端到端 ΔG 预测 wrapper |
| `cross_benzoin/eval_round10_al_benefit.py` | round10 AL 消融（null 结果） |
| `data/cross_benzoin/homo_v6/viz_gxtb_20260625/confnoise_summary_20260626_1559.md` | homo 的构象噪声研究（~2.14 地板，本次 cross 复现 ~2.3） |
| memory `~/.claude/.../memory/*.md` | `homo-active-relabel-null-result`、`dft-arbitration-result`、`benzoin-dg-repo-moved-and-data-loss`、`scratch1-inode-nodespec-orphans` 等 |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit -F <msg 文件>`。
   git 卡就在本文件第 4 节精确写清「已 staged 什么、msg 在哪、接手第一件事」。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根），用本文件这套小节结构：
   `0 一句话` / `1 立即行动`（带确切命令）/ `2 在跑的 SLURM 作业`（表格 + 标注别碰的）/
   `3 会话内监控`（**强调随会话失效，给重建命令**）/ `4 未提交 git` / `5 关键决策` /
   `6 已知陷阱` / `7 文档索引` / `8 本节（套路自身，复制过去）`。
3. **`RUN_LOG_<date>.md` 收尾**：最后一行进度快照 + 任何半截状态。
4. **更新 memory**：`MEMORY.md` 里指向新 HANDOFF 的一行；非显然的新事实写成 memory 文件。
5. **产出 resume prompt**（贴给新会话），模板见下。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（窗口 B：cross-benzoin dG）。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_20260904.md，第 0-1 节是立即行动。
2. 会话内监控全失效了 —— 按第 3 节用 Monitor 工具重建。
3. squeue -u schen3 对齐第 2 节作业表；确认 dg_geom_method 26371408 是否完成，做第 1.2-1.3 节的分叉。
4. 并行的 NHC 窗口(schen3-bc)、BDE 自动化、mace-delta-train、orcasp_INT2_* 不要碰。
5. 进展记进 RUN_LOG_20260903.md；会话结束按第 8 节套路交接。
```
