# 交接文档 — 2026-09-04（晚间版，取代同日早先版本）

> 写给下一个接手的会话。**先读第 0-1 节，再读第 3 节重建监控。**
> 本会话已**接管整个项目**（cross-dG + BDE，取代此前"窗口 A/B 分工别碰"的旧约定）——
> 只有 NHC（schen3-bc / schen3-7a）、`mace-delta-train`、`orcasp_INT2_*` 仍然不碰。
> 本文档结构是**固定套路**，见第 8 节。用户即将断网回家，这是断网前的完整快照。

---

## 0. 一句话

**champion 仍是 r1-10**（blend MAE 2.215，`CHAMPION.md` 冻结，未变）。今天下午做了
三件大事：(1) **接管整个项目**（BDE 自动化线也在本会话驱动了）；(2) 产出了**事无
巨细的项目完成总结**——`PROJECT_SUMMARY_20260904.md`（repo）+ 一份 Artifact 报告页
（链接见第 7 节）；(3) 周末推 MAE 的实验：**分类/排序重表述验证为正**（AUC
0.92-0.93，今天就能交付，不受标签噪声天花板限制），**决定性几何偏差消融
`dg_geom_method` 即将出结果**（57/60，本文档写作时还差 2 个 task）。

**现在最重要的下一步**：等 `dg_geom_method 26371408` 完成（Monitor `bfjq6n1fb` 会报,
但**随会话结束失效**，见第 3 节重建），读 hetero vs control 组的中位 ddG：
- 若 hetero 组 ddG 中位数 >1 kcal、明显 >> control、且 rmsd_prod_med <0.3（干净）
  → 人工搭建 + 提交定向 g-xTB 几何精修 + r2SCAN 重标签战役（~5,000-8,000 个杂原子重
  的对），**不要无人值守自动触发**——这是个新流水线，第一次跑要看中间结果。
- 若无偏差 → 标签质量调查到此为止，全力转向 Goal 3（部署 `predict_dg.py` + 已验证
  的分类/排序重表述）。
- 判读用的现成代码见 `PROJECT_SUMMARY_20260904.md` §3.9 和早先版 HANDOFF 的 §1.2
  （聚合 `data/cross_benzoin/dg_geom_method/chunks/dgm_*.csv` 的 `ddG_kcal`/`grp`/
  `rmsd_prod`）。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.20j %.9P %.2t %.10M %R" | grep -vE "nhc-|_cron|int2|orcasp_INT2|mace"
/usr/hpc/bin/myquota | grep -A3 wstor_scratch1        # inode，>90% 才需要动手（现 ~25%）
git log --oneline -5 ; git status --short             # git 会间歇卡，见第 4 节
tail -100 RUN_LOG_20260903.md                         # 本次会话的完整逐步日志
```

### 1.2 `dg_geom_method 26371408` 出结果后
```
python3 - <<'PY'
import glob,csv,statistics as st
h=[];c=[];rp=[]
for f in glob.glob('data/cross_benzoin/dg_geom_method/chunks/dgm_*.csv'):
  if 'stale' in f: continue
  for r in csv.DictReader(open(f)):
    v=r.get('ddG_kcal')
    if v and abs(float(v))<1e4:
      (h if r.get('grp')=='hetero' else c).append(float(v))
      if r.get('rmsd_prod'): rp.append(float(r['rmsd_prod']))
print(f'n={len(h)+len(c)}/60  hetero med {st.median(h):+.2f}(n{len(h)})  ctrl med {st.median(c):+.2f}(n{len(c)})  rmsd_prod med {st.median(rp):.2f}')
PY
```
按 §0 的判读分叉。若 hetero 组样本里有一个是缺失的（第 001 分片的 stale-schema
bug，见 RUN_LOG 09-04 14:10 条目），最多缺 1/35 个 hetero 点，需要的话补跑：
```
$PY cross_benzoin/dg_geom_method_worker.py --sample data/cross_benzoin/dg_geom_method/sample.csv \
   --skip 1 --max 1 --out data/cross_benzoin/dg_geom_method/chunks/dgm_000_01.csv --scratch /tmp/cn_catchup
```
（`$PY` = `/home/schen3/venv/nhc-workflow/bin/python`）

### 1.3 BDE featurize array 出队后
`bde_post_sweep`（`26326313`）挂 `afterany` 依赖两个 featurize array，**会自动**跑
完整度检查 → 重组两个描述符库 → 备份 → 提交完整 model sweep（B6 ckpt + deep
ensemble + B4/B5 诚实重训 + GBM bake-off + Phase-3 3D）。不需要手动干预，等它跑完后
核对结果、`git add -f`、更新 `pipeline/bde/STATUS.md` 排名表（见
`pipeline/bde/POST_ARRAY_MODEL_SWEEP.md` 完整清单）。

### 1.4 收尾
- 三线都收齐后统一 push 分支 `agent/recovery-20260902`（领先 origin 全部本 repo
  历史，92+ commits，从未 push 过）。
- 若做了定向重标签（§0 第一支）：重训 r1-10 变体，在 B/S/P 富集 holdout 上和现
  champion 对比，更新 `CHAMPION.md`。

---

## 2. 在跑 / 相关的 SLURM 作业（09-04 ~16:00）

| jobid | 名 | 干什么 | 状态 | 完成后 |
|---|---|---|---|---|
| **`26371408`** | `dg_geom_method` | 决定性三物种 ΔΔG 几何方法消融（60 对） | 🏃 57-58/60，2 task 剩 | **见 §0/1.2，主线** |
| `26368077` | `confnoise_cross` | 构象噪声地板 | ✅ DONE（32/32，dG_std median 2.884） | 结论已入 PROJECT_SUMMARY |
| `26368749` | `geom_bias` | GFN2 vs g-xTB 产物侧能量偏差 | ✅ DONE（结构化于P/SO2/B） | 同上 |
| `26316404` | `bde_homoprod`（genoa） | BDE 220k featurize | 🏃 features.csv 1866/2209（~85%），throttle 40 | `26326313` 自动 |
| `26324801` | `bde_homoprod_rome`（rome） | 同上 | 🏃 throttle 100 | 同上 |
| `26326313` | `bde_post_sweep` | array 完成后自动 assemble+sweep | PD Dependency | 自动 |
| `26373559` | `bde_geomarch` | BDE 几何增量归档（本会话重投，原 `26348174` 撞 24h 墙钟过期） | 🏃 fat_rome | 自退（array 出队后）或本会话监控自动重投 |
| `26373560` | `clean_orphans` | scratch inode 孤儿清理（本会话重投，原 `26348175` 同上过期） | 🏃 fat_rome | 常驻，本会话监控自动重投 |

**不是我的、别碰**：所有 `nhc-*` / `*_cron`（NHC 窗口，今天是 `schen3-7a`，已互相确认
边界）、`mace-delta-train`、`orcasp_INT2_*` / `int2-*`。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

用 Monitor 工具重建（persistent，timeout 3600000）：

1. **`dg_geom_method 26371408` 完成** → 聚合 hetero/ctrl 中位 ddG + rmsd_prod_med
   （命令见 §1.2）。本会话 = `bfjq6n1fb`（脚本
   `.../scratchpad/mon_labelprobes.sh`，30 min 轮询）。confnoise 已完成，脚本里
   那部分逻辑不用管了。
2. **BDE featurize array 出队 + geomarch/janitor 存活看护**：本会话 = `bbzu8ozjc`
   （脚本 `.../scratchpad/mon_bde.sh`）。两个 array 出队时报 `*** BDE FEATURIZE
   ARRAYS DONE`；同时每 30 min 检查 `bde_geomarch`/`clean_orphans` 是否还在队列里，
   连续 2 次探测不到（间隔 ≥90 min）就自动 `sbatch --partition=fat_rome` 重投
   —— **这两个服务会撞 fat_rome 的 24h 墙钟，新会话如果不重建这个监控，务必自己
   记得每天手动检查一次 `squeue -u schen3 -n bde_geomarch,clean_orphans`**。
3. **scratch1 inode 阈值**：本会话 = `b3sbh4gba`（脚本 `.../scratchpad/mon_inode.sh`），
   只在跨 75/90/100% 时报。现 ~25%，安全。
4. 三个脚本都在 `/gpfs/scratch1/nodespecific/int6/76981/claude-76981/-gpfs-scratch1-shared-schen3-benzoin-dg/87f59f85-3268-4303-b7b4-86884e27c674/scratchpad/mon_*.sh`
   ——**这是本次会话的临时目录，可能在会话结束后被清理，不保证长期存在**。新会话
   照抄里面的轮询逻辑（都很短，各 30-50 行 bash）比假设文件还在更可靠。

---

## 4. 未提交 / 未 push 的 git

- HEAD = `f17e0ca`（`PROJECT_SUMMARY_20260904.md` + 分类/排序重表述脚本已 commit）。
- `git status --short` 干净相对于本会话的改动——只剩长期存在的、之前会话就有的
  未 track 数据目录（`data/cross_benzoin/{dg_geom_method,geom_bias,confnoise_cross,
  wb97x_shift,r89_aldehyde_regen,...}/` 等，探针/中间产物，小，按项目惯例随意
  commit 或留着不管）和 round9 模型文件的 `M`（其他会话训练产出，不是本会话改的，
  别乱 commit/discard）。
- 分支 `agent/recovery-20260902`，领先 origin **全部**本 repo 历史（92+ commits），
  从 2026-07-13 fresh history 起就从未 push，等三线收齐统一 push（见 §1.4）。
- git 在这个 GPFS mount 上间歇卡（`git status` 偶尔要 30-60s），`rm -f
  .git/index.lock` 后重试。`git log` 从 pack 读，快，不受影响。

---

## 5. 关键决策 & 不要重新讨论的事

- **champion = r1-10**（blend MAE 2.215）。r1-9（2.167）是 fallback。理由见
  `CHAMPION.md`：数据更多 + AL 难例覆盖 + blend 优势更强（w_gnn 0.40→0.50）。
- **round10 AL（2k 规模）= null**。`eval_round10_al_benefit.py`：B−A = −0.033（噪声内）。
  **不要再提 2k 规模的 AL 轮**。
- **多构象 Boltzmann 重标签 = null**（cross 项目第 3 次独立确认，homo 已 2 次）。
- **单构象 r2SCAN-3c 标签噪声地板 ≈ 2.9 kcal/mol**——champion MAE 已贴地板。继续加
  同类数据 / 同类模型容量的边际收益已经很低（07-21 的 4 种 3D-GNN 架构对比也挤在
  2.177-2.188，独立佐证）。
- **分类/排序重表述 = 正**（09-04 新结果，不依赖几何偏差消融的结果）：AUC
  0.92-0.93，top-10% 精度 0.644 vs g-xTB 0.311。这是一个已验证、可以直接向用户
  交付的 Goal-3 产出，脚本 `cross_benzoin/eval_reformulation_classification_ranking.py`，
  数字在 `data/cross_benzoin/reformulation_classification_ranking_eval.json`。
- **本会话起，全项目由单会话统一驱动**（用户 09-04 ~14:50 指示），BDE 自动化线的
  "别碰"限制已解除。NHC/mace/INT2 仍然不碰。
- **不要盲目开更大规模（5-10k）的 AL 批次**——三条独立证据（round10 null、标签
  噪声地板、07-21 三种 3D 架构 null）都指向"同类数据+模型容量已到平台期"。除非
  几何偏差消融给出正面结果（走定向重标签路线）或用户明确要求，否则默认不做。

---

## 6. 已知陷阱

- **BDE 的 `bde_geomarch`/`clean_orphans` 会撞 fat_rome 24h 墙钟**——脚本头部写的
  `staging` 分区分区 fairshare 触底排不上，只能用 fat_rome，但那里有 24h 硬顶。
  两个服务本身不会自重投，必须靠外部监控（见 §3.2）或人工每天检查。
- **`dg_geom_method` v1 的坑**：GFN2 和 g-xTB 各自独立优化会落进不同构象，ddG 变成
  构象噪声而非几何方法偏差（v1 rmsd_prod 0.87 Å）——正确做法是 g-xTB 从 GFN2 极小点
  出发**精修**，不独立重新搜索。当前跑的版本（`26371408`）已经这样做，rmsd_prod_med
  0.20 Å 验证过修复生效。
- **`dg_geom_method/chunks/dgm_000_01.csv` 曾是一个 schema 不对的 stale 文件**
  （已改名 `.stale_foreignschema`），导致 array task0/worker1 在启动时被跳过。
  聚合结果时留意这个分片是否需要补跑（§1.2）。
- **`scontrol update JobId=... ArrayTaskThrottle=N`** 在这个集群上对已实体化的
  pending 子任务会报 "Access/permission denied"，但对 base array 本身确实生效——
  别被那些报错吓到，用 `scontrol show job <id> | grep ArrayTaskThrottle` 验证。
- 其余陷阱（g-xTB 路径硬编码、float-id join bug、ORCA SCF 关键字、GPFS git 卡顿等）
  见 `PROJECT_SUMMARY_20260904.md` 和更早的 HANDOFF/RUN_LOG，未再重复。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **`PROJECT_SUMMARY_20260904.md`**（repo 根，新） | **事无巨细的项目完成总结**，按时间顺序，AL 为主线，三条线全覆盖。一次性快照，不是 living doc |
| Artifact「Ten Rounds to the Noise Floor」 | 上面文档的可视化精简版，`Artifact action:list` 或问用户要链接 |
| `RUN_LOG_20260903.md` | 本次会话的完整逐步日志（09-03 起，living doc） |
| `HANDOFF_20260903.md` / 更早版本的 `HANDOFF_20260902.md` | 更早的交接快照 |
| `cross_benzoin/CHAMPION.md` | 当前 champion 加载方式、数字、lineage |
| `pipeline/bde/STATUS.md` + `POST_ARRAY_MODEL_SWEEP.md` | BDE 子课题入口 + 阵列后计划 |
| `data/cross_benzoin/reformulation_classification_ranking_eval.json` | 分类/排序重表述完整数字 |
| memory `~/.claude/.../memory/*.md` | `cross-r1-10-champion-and-label-ceiling`（今天更新了取代关系）、`concurrent-bde-session-20260902`（已标 OBSOLETE） |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit -F <msg 文件>`。
   git 卡就在本文件第 4 节精确写清「已 staged 什么、msg 在哪、接手第一件事」。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根，同一天多次交接就覆盖当天这份），用本文件
   这套小节结构：`0 一句话` / `1 立即行动`（带确切命令）/ `2 在跑的 SLURM 作业`
   （表格 + 标注别碰的）/ `3 会话内监控`（**强调随会话失效，给重建命令**）/
   `4 未提交 git` / `5 关键决策` / `6 已知陷阱` / `7 文档索引` / `8 本节（套路自身，复制过去）`。
3. **`RUN_LOG_<date>.md` 收尾**：最后一行进度快照 + 任何半截状态。
4. **更新 memory**：`MEMORY.md` 里指向新 HANDOFF 的一行；非显然的新事实写成 memory 文件。
5. **产出 resume prompt**（贴给新会话），模板见下。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（全项目：cross-benzoin dG 主线 + BDE 自动化，单会话统一驱动）。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_20260904.md，第 0-1 节是立即行动。
2. 会话内监控全失效了 —— 按第 3 节用 Monitor 工具重建（含 BDE geomarch/janitor 的存活看护，别漏）。
3. squeue -u schen3 对齐第 2 节作业表；确认 dg_geom_method 26371408 是否完成，做第 1.2 节的判读分叉。
4. 确认 bde_geomarch/clean_orphans 两个服务还在队列里（会撞 fat_rome 24h 墙钟，可能需要重投）。
5. 并行的 NHC 窗口(schen3-bc/schen3-7a)、mace-delta-train、orcasp_INT2_* 不要碰。
6. 进展记进 RUN_LOG_20260903.md（或当天新建一份）；会话结束按第 8 节套路交接。
```
