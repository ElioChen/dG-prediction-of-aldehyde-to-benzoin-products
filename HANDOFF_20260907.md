# 交接文档 — 2026-09-07（取代 HANDOFF_20260904.md）

> 写给下一个接手的会话。**先读第 0-1 节，再读第 3 节重建监控。**
> 本会话继续"单会话统一驱动整个项目"（cross-dG + BDE），沿用 09-04 起的约定。
> 只有 NHC（schen3-bc / schen3-7a）、`mace-delta-train`、`orcasp_INT2_*` 不碰。
> 本文档结构是固定套路，见第 8 节。

---

## 0. 一句话

**champion 仍是 r1-10**（blend MAE 2.215，未变）。今天（09-07）做的事：(1) 09-04
留的悬念全部收尾——`dg_geom_method` 判读为**无几何偏差**，标签质量调查结案；
(2) BDE 全量重建+model sweep 补完收尾，**新 BDE champion：B6 deep ensemble，醛
1.851 / 产物 2.826**（旧数字 1.579/3.060 作废）；(3) Goal 3 部署 `predict_dg.py`
时冒烟测试发现并修复了一个**严重的跨子项目 bug**（BDE 库重建让它对任何新分子对
100% 失败）；(4) 分类/排序重表述做成了 `predict_dg.py` 的默认输出列；(5) 发现
并正在补算一个数据缺口（醛侧整分子 `G_gxtb`，array 还没跑完）；(6) 写了两份新
文档（`PROJECT_SUMMARY_20260904.md` 增补 + 新的 `ALDEHYDE_LIBRARY_AND_DG_
WORKFLOW_20260907.md`）；(7) 全部代码已 push 到 GitHub（本仓库历史首次推送）。

**现在最重要的下一步**：等 `26432805`（G_gxtb 补算 array）跑完，跑
`pipeline/bde/merge_aldehyde_gxtb.py` 把结果并回 `aldehydes_all.csv`，验证覆盖率
提升，然后可以考虑收尾这条线（不是阻塞性任务，`predict_dg.py` 现在已经能正常
工作，G_gxtb 只是让 2/260 特征从"缺失"变成"有真实值"，边际收益不大）。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.20j %.9P %.2t %.10M %R" | grep -vE "nhc-|_cron|int2|orcasp_INT2|mace"
/usr/hpc/bin/myquota | grep -A3 wstor_scratch1        # inode，>75% 才需要留意（现 ~17.7%）
git log --oneline -5 ; git status --short             # 本会话结束时全部已 push，理论上 clean
tail -150 RUN_LOG_20260903.md                         # 本次会话完整逐步日志
sinfo -s                                              # 09-07 白天 genoa 分区整个 drain/down，
                                                       # 新作业前务必先查这个，别往坏分区提交
```

### 1.2 G_gxtb 补算 array（`26432805`）状态判读
```
sacct -j 26432805 -X --format=State -n | sort | uniq -c
squeue -h -j 26432805 | wc -l
```
- 若 `squeue` 里已经空了（跑完了）：
  ```
  PY=/home/schen3/venv/nhc-workflow/bin/python
  $PY pipeline/bde/merge_aldehyde_gxtb.py --dry-run   # 先看覆盖率提升多少，再去掉 --dry-run 真写
  ```
  合并后可选：重新跑一次 `predict_dg.py` 的 20-pair smoke test（见 §6 陷阱一）
  确认 G_gxtb 覆盖率提升没有引入新问题。**这条线做完可以算收尾，不必强求 100%
  覆盖**——4/2200 chunk 是原始 featurize 阶段就已知的不可恢复损失（§6）。
- 若还没跑完：不用管，非阻塞。继续观察即可，失败率一直稳定在 ~1-1.5%，全部是
  已知的两类原因（§6），没有新失败模式的话不需要排查。

### 1.3 `bde_geomarch`/`clean_orphans` 存活检查
两个服务目前的行为模式（09-07 观察到、多次重复）：**`bde_geomarch` 每次被重投
后只需要 2-4 分钟就正常 COMPLETE 退出**（因为两个 featurize array 早就跑完了，
没什么可归档的了，它做一遍扫描确认没有新东西就退出）——这是**预期行为，不是
故障**，liveness monitor 会持续看到"miss → 重投 → 很快又 miss"的循环，属于良性
噪音，不需要人工干预。`clean_orphans` 是真正持续在跑的（`while true` 循环），
应该会一直在 squeue 里看到。若 `clean_orphans` 也消失了才需要真正关注（见
§3 重建监控里的自动重投逻辑）。

---

## 2. 在跑 / 相关的 SLURM 作业（09-07 ~12:11）

| jobid | 名 | 干什么 | 状态 | 完成后 |
|---|---|---|---|---|
| **`26432805`** | `ald_gxtb` | G_gxtb 补算 array（2200 chunk，`rome` 分区） | 🏃 ~2016/2200 完成（92%），25 fail（已知原因） | 见 §1.2 |
| `26397167` | `clean_orphans` | inode 孤儿清理，持续服务 | ✅ 健康在跑，`fat_rome` | 继续跑，不用管 |
| `26373560` | `clean_orphans` | 同上（**两个实例并存**，冗余但无害，是之前重投时留下的，没必要去杀掉任何一个） | ✅ 健康在跑 | 同上 |
| `26326313` 及其后续 sweep job | `bde_post_sweep` 全家桶 | BDE 09-06 已全部完成 | ✅ DONE，见 §0 | 已收尾，不需要动作 |

**不是我的、别碰**：所有 `nhc-*` / `*_cron`（NHC 窗口）、`mace-delta-train`、
`orcasp_INT2_*` / `int2-*`。

**09-07 集群健康提醒**：白天检查时 `genoa` 分区**整个 drain/down**（737 节点全
不可用），`fat_genoa` 大半 down，`gpu_h100` 全部 drain。提交任何新作业前先
`sinfo -s` 确认目标分区没有整体故障（不是"忙"，是"down"），genoa 曾经是 BDE
featurize 的默认分区，09-07 之后新作业改投 `rome`/`fat_rome`，除非确认 genoa
已恢复。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

用 Monitor 工具重建：

1. **G_gxtb array 完成 + 里程碑进度**：本会话 = `b5jbop08e`。**教训：不要每
   60 秒就报一次进度**（第一版监控这样写，几十条通知刷屏，用户反馈"生成太多
   routine noise"）——改成只在跨 200 个任务里程碑、失败数变化、或完全跑完时才
   report，轮询间隔拉到 600 秒。新会话重建时直接抄这个节流逻辑：
   ```bash
   # 伪代码，见本会话历史里 b5jbop08e 的实际脚本
   while true; do
     n_done=$(sacct -j 26432805 -X --format=State -n | grep -c COMPLETED)
     n_fail=$(sacct -j 26432805 -X --format=State -n | grep -c FAILED)
     n_pend=$(squeue -h -j 26432805 | wc -l)
     milestone=$((n_done/200))
     [ "$milestone" -gt "$last_milestone" -o "$n_fail" -gt "$last_fail" ] && echo "progress: done=$n_done fail=$n_fail" && last_milestone=$milestone && last_fail=$n_fail
     [ "$n_pend" -eq 0 -a "$n_done" -gt 0 ] && echo "*** FULLY DRAINED ***" && exit 0
     sleep 600
   done
   ```
   若这个 array 已经跑完（见 §1.2），这个监控就不用重建了，直接做 §1.2 的合并
   动作即可。
2. **`bde_geomarch`/`clean_orphans` 存活看护**：本会话 = `bcsxqdjqe`（脚本
   `pipeline/slurm/` 里没有对应文件，是本会话现场写的 bash，逻辑很短：每 30
   分钟查一次 `squeue -u schen3 -n bde_geomarch/clean_orphans`，连续 2 次探测
   不到（间隔 ≥30 分钟）就 `sbatch --partition=fat_rome pipeline/bde/
   archive_completed_chunk_geoms.sh` 或 `clean_orphan_nodespec_workdirs.sh`
   重投）。**注意 §1.3 提到的良性 miss/resubmit 循环**，新会话重建这个监控后
   会持续看到这种噪音，属预期，不代表哪里坏了。
3. **inode 阈值**：本会话 = `bjz75wlx2`，只在跨 75/90/100% 时报。现 17.7%，
   安全。09-04 有过一次 94.4% 峰值自愈的先例（见 `RUN_LOG_20260903.md` 09-04
   ~20:05 条目 + memory `scratch1-inode-nodespec-orphans`），如果再复发不要
   慌，throttle-down（`scontrol update JobId=<array> ArrayTaskThrottle=N`，哪怕
   报 "Access/permission denied" 的噪音也是生效的，验证方法见那条 memory）通常
   就够了，不一定需要 `scancel`（而且 `scancel` 大概率会被 Claude Code 的
   auto-mode 分类器拦截，届时直接跟用户说清楚在做什么、为什么需要，不要试图
   绕过去）。

监控脚本这次没有额外写文件到 scratchpad（都是内联在 Monitor 工具调用里的
bash），新会话按上面贴的逻辑现写即可，不依赖任何临时目录里的文件是否还在。

---

## 4. 未提交 / 未 push 的 git

**干净——本会话结束时全部已提交并推送。**
```
git log origin/agent/recovery-20260902..HEAD --oneline | wc -l   # 应该是 0
```
- HEAD = `4cbacfa`（本会话最后一次 commit）。
- **09-07 是这个仓库历史第一次真正 push 到 GitHub**（`git@github.com:ElioChen/
  dG-prediction-of-aldehyde-to-benzoin-products.git`，分支
  `agent/recovery-20260902`）——之前 92+ commit 一直只在本地。之后新 commit
  记得随手 `git push`，不要again攒一大批。
- `git status --short` 剩下的都是长期存在的、之前会话留下的东西：`FILE_MAP.md`
  一处未提交改动（不是本会话做的，别乱 commit/discard）、round9 模型文件的
  `M`（其他会话训练产出）、一堆探针/中间产物的未跟踪目录（`data/cross_benzoin/
  {dg_geom_method,geom_bias,confnoise_cross,...}/`，小，按项目惯例随意）。

---

## 5. 关键决策 & 不要重新讨论的事

- **champion = r1-10**（blend MAE 2.215）。不变。
- **几何方法偏差消融 = 无偏差**（09-04 收官，09-07 无新证据推翻）。标签质量
  investigation 到此为止，不要再提定向重标签战役，除非有新证据。
- **BDE champion 现为 B6 deep ensemble，醛 1.851 / 产物 2.826**（09-06 全量
  220k 重训，取代旧的 42k 局部库上跑出的 1.579/3.060——数据规模不同，不能直接
  比较优劣，但对外一律引用新数字）。
- **round10 AL（2k 规模）= null，多构象重标签 = null，标签噪声地板 ≈2.9
  kcal/mol** ——这三条 09-04 前就确认的结论依然成立，不要重新验证。
- **Phase-3 3D GNN（BDE）不跑**：查证后发现它其实是 dG 项目的脚本（预测
  `dG_orca-dG_gxtb`，不是 BDE 目标），而且这个架构族已经在 dG 任务上验证过
  null（07-21，4 个架构 MAE 挤在 2.177-2.188）。除非用户明确要求，不要主动
  提议重跑或改造它。
- **分类/排序重表述已经是 `predict_dg.py` 的标准输出**（`dg_favorable`/
  `dg_below_train_median`/`dg_rank_pct` 三列），不是一个单独的、需要额外运行的
  评估脚本了。
- **`aldehydes_clean_v6.csv`（醛结构库本身）一直安全，在 git 里，不需要额外
  备份操心**——真正需要盯的是它下游的局部/全局描述符库（`aldehydes_all.csv`
  等），这些体积大、gitignore、是 07-20 purge 唯一真正丢过东西的那一层，细节
  见新文档 `ALDEHYDE_LIBRARY_AND_DG_WORKFLOW_20260907.md`。

---

## 6. 已知陷阱

- **共享库文件的跨子项目耦合是真实存在的坑**：`data/cross_benzoin/homo_v6/
  aldehydes_all.csv`/`products_all.csv` 被 BDE 和 cross-dG 两个子项目同时读写，
  09-06 BDE 重建时选的覆盖率哨兵字段（`G_gxtb`）恰好是 BDE 不需要、cross-dG
  需要的量，静默打断了 `predict_dg.py`——**以后任何改这两个共享文件 schema 的
  工作，改完都要跑一次 `predict_dg.py` 的 smoke test**（20 个已知分子对，见
  `cross_benzoin/CHAMPION.md` 的命令），不要假设"BDE 那边测过了就没事"。完整
  记录：memory `predict-dg-g-gxtb-regression-fixed`。
- **BDE `bde_geomarch` 重投后很快自己退出是正常的**（§1.3），不要误判成故障
  反复重投——两个 featurize array 都已经跑完，没有持续的归档需求了，这个服务
  的使命已经基本完成，长期来看甚至可以考虑不再自动重投（这次会话为了保险还是
  留着自动重投逻辑，下次会话可以酌情评估要不要干脆停掉）。
- **`ArrayTaskThrottle` 的 "Access/permission denied" 噪音是无害的**（对已实体
  化的 pending 子任务），base array 本身确实生效，用
  `scontrol show job <id> | grep ArrayTaskId` 验证真实throttle，不要被吓到。
- **`scancel` 会被 Claude Code 的 auto-mode 分类器拦截**（09-04 亲测两次），
  `scontrol update`（throttle 调整）不会。需要真的 kill 掉某个作业时，先说明
  意图让用户决定，不要试图用其他工具绕过分类器。
- **提交新 SLURM 作业前先 `sinfo -s` 查一遍目标分区状态**——09-07 genoa 整个
  drain/down，如果不查直接照抄旧脚本里的 `--partition=genoa` 会导致作业排队
  排到天荒地老还以为是"忙"。
- **临时文件必须写到共享 GPFS 路径，不能用提交节点本地 `/tmp`**——本会话一次
  array 提交的 manifest 文件图省事写到了 `/tmp`，导致前 8 个 task 瞬间全部
  FAILED（计算节点看不到提交节点的本地 `/tmp`），换成仓库内路径就好了。
- 其余更早的陷阱（g-xTB 路径硬编码、float-id join bug、ORCA SCF 关键字、GPFS
  git 卡顿、scratch1 inode 孤儿等）见 `PROJECT_SUMMARY_20260904.md`、
  `ALDEHYDE_LIBRARY_AND_DG_WORKFLOW_20260907.md` 和更早的 HANDOFF/RUN_LOG，
  未再重复。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **`PROJECT_SUMMARY_20260904.md`**（09-07 增补版） | 事无巨细、按时间顺序、AL 为主线的项目总结，09-07 补入了 §3.9 最终判决/§4.4-4.5 BDE 收官+跨项目 bug/§7.2 现状快照。用户拿它写工作汇报，**这次是例外允许增量编辑**，之后原则上还是恢复"快照不改"惯例 |
| **`ALDEHYDE_LIBRARY_AND_DG_WORKFLOW_20260907.md`**（新，09-07） | 技术参考文档（不是时间线）：醛库构建历史+备份现状、dG 工作流全流程、描述符工程细节、模型架构、当前 G_gxtb 缺口。可以直接增量更新，不用另开新版 |
| `RUN_LOG_20260903.md` | 逐步日志（09-03 起，living doc，本会话全部细节都在这里） |
| `HANDOFF_20260904.md` / 更早版本 | 更早的交接快照 |
| `cross_benzoin/CHAMPION.md` | champion 加载方式、数字、lineage；09-07 已更新记录 G_gxtb bug 和新输出列 |
| `pipeline/bde/STATUS.md` | BDE 权威入口；09-07 已更新 §2.1b 新冠军数字 + Phase-3 3D 的查证结论 |
| `data/cross_benzoin/reformulation_classification_ranking_eval.json` | 分类/排序重表述完整数字 |
| memory `~/.claude/.../memory/*.md` | 09-07 新增/更新：`predict-dg-g-gxtb-regression-fixed`（新）、`bde-scaffold-disjoint-retrain-submitted`（数字更新）、`bde-post-purge-asset-state`（标记已解决）、`scratch1-inode-nodespec-orphans`（补充 throttle 确实生效的教训）、`cross-r1-10-champion-and-label-ceiling`（几何偏差判决更新） |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit -F <msg 文件>`。
   git 卡就在本文件第 4 节精确写清「已 staged 什么、msg 在哪、接手第一件事」。
   **记得 `git push`**——09-07 起这个仓库有远程了，不要再攒着不推。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根，同一天多次交接就覆盖当天这份），用本文件
   这套小节结构：`0 一句话` / `1 立即行动`（带确切命令）/ `2 在跑的 SLURM 作业`
   （表格 + 标注别碰的）/ `3 会话内监控`（**强调随会话失效，给重建命令**）/
   `4 未提交 git` / `5 关键决策` / `6 已知陷阱` / `7 文档索引` / `8 本节（套路自身，复制过去）`。
3. **`RUN_LOG_<date>.md` 收尾**：最后一行进度快照 + 任何半截状态。
4. **更新 memory**：`MEMORY.md` 里指向新 HANDOFF 的一行；非显然的新事实写成 memory 文件。
5. **产出 resume prompt**（贴给新会话），模板见下。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（全项目：cross-benzoin dG 主线 + BDE 自动化，单会话统一驱动）。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_20260907.md，第 0-1 节是立即行动。
2. 会话内监控全失效了 —— 按第 3 节重建（G_gxtb array 进度 + geomarch/janitor 存活看护 + inode）。
3. squeue 对齐第 2 节作业表；判读 G_gxtb 补算 array(26432805) 是否跑完，跑完的话按第 1.2 节合并结果。
4. 提交任何新 SLURM 作业前先 sinfo -s 查分区健康（09-07 genoa 整个 down 过）。
5. 并行的 NHC 窗口(schen3-bc/schen3-7a)、mace-delta-train、orcasp_INT2_* 不要碰。
6. 进展记进 RUN_LOG_20260903.md（或当天新建一份）；会话结束按第 8 节套路交接，记得 git push。
```
