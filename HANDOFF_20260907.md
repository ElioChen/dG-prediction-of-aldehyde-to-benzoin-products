# 交接文档 — 2026-09-07（傍晚版，取代当天早先的版本）

> 写给下一个接手的会话。**先读第 0-1 节。** 本会话继续"单会话统一驱动整个项目"
> （cross-dG + BDE）。只有 NHC（`schen3-bc` / `schen3-7a`）、`mace-delta-train`、
> `orcasp_INT2_*` 不碰。本文档结构是固定套路，见第 8 节。

---

## 0. 一句话

**champion 仍是 r1-10（blend MAE 2.215，未变）。** 本会话（09-07）做完的事：
1. G_gxtb 补算收尾（覆盖率 99.07%）；
2. **A/B/C/D "开放线复盘"四项全部落地**（用户在质疑"项目静止"后点的）——
   - **A**：`predict_dg.py` 部署健壮性（split-conformal 90% 区间 + `baseline_risk`
     杂原子旗标 + `dg_high_sigma` OOD 守卫），已 push；
   - **B**：homo/cross ΔG 差异定量分解——**差异 = 切分口径 + 数据规模，不是任务难度
     或标签质量**（对等条件下 cross ≈ homo）；
   - **C**：homo+cross 联合 tabular = **AMBER-GREEN**（naive_merge 在两个模型类上都
     +0.11 kcal，finetune null）；
   - **D**：便宜基线 pilot（128/128）= **AMBER-GREEN 倾向 GREEN**——换 B97-3c 当
     Δ-learning 基线，残差 scatter std **4.32 → 1.11**（比值 0.26），**可能是几个月
     来第一个真正的精度杠杆**；
3. 新版 `PROJECT_SUMMARY_20260907.md`（中）+ `_EN.md`（英），围绕 AL 主线 + ΔG 预测
   重组，图文并茂（7 张 PNG + mermaid），取代 09-04 版作汇报底稿；
4. `ALDEHYDE_LIBRARY_AND_DG_WORKFLOW_20260907_EN.md`（英文版）。

**本会话末尾给出了三条针对未来工作的具体建议（§1.2）——下个会话的主线就是推进
这三条。** 全部代码/文档/图已 push。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.20j %.9P %.2t %.10M %R" | grep -vE "nhc-|_cron|int2|orcasp_INT2|mace"
/usr/hpc/bin/myquota | grep -A3 wstor_scratch1        # inode，现 ~20.4%，安全
git log --oneline -5 ; git status --short             # 本会话结束时全部已 push
tail -200 RUN_LOG_20260903.md                         # 本会话逐步日志
sinfo -s                                              # 09-07 傍晚所有分区已恢复 up；新作业前仍先查
```
- **在跑的本项目作业：无。** 只有 1 个 `clean_orphans`（`26397167`，janitor，健康）
  + NHC 作业（别碰）。
- git：HEAD `f4ebb1d`，全部已 push（`agent/recovery-20260902` 分支）。`git status`
  剩的都是长期存在的（`FILE_MAP.md` 一处、round9 模型 `M`、一堆探针/中间产物未跟踪
  目录，含 `data/cross_benzoin/cheap_baseline_pilot/chunks/`——D 的 128 个碎 csv，
  merged.csv + summary.json 已提交，chunks 本身不必提交）。

### 1.2 三条建议 —— 下个会话的主线

三条互补：**建议 1 = 精度天花板本身（换标签基线），建议 2 = 数据效率+工程统一，
建议 3 = AL 战略转向。** 完整论证见本会话对话记录 + `PROJECT_SUMMARY_20260907.md
§6` + `data/analysis/homo_cross_gap/FINDING.md`。

#### 建议 1：把 B97-3c 基线杠杆做实（最高优先级，唯一有量级收益的方向）

**依据**：D pilot（128/128）——换 B97-3c 当 Δ-learning 基线，残差 scatter std
**4.32 → 1.11**（比值 0.26）。过去几个月所有"推 in-domain MAE"的尝试全部 null
（round10 AL / 多构象重标签 / 3D-GNN / 几何方法），因为都在标签噪声地板上打转。
B97-3c 是第一个动"基线本身"、可能真正下压 champion MAE 2.215 的杠杆。

**执行步骤**：
1. **生产几何复核**（~1 天，30 对）：拿 30 对在**生产 funnel_v3 几何**（不是 pilot
   的一次性 ETKDG/GFN2）上重算 B97-3c + r2SCAN-3c，确认 std 比值 0.26 不是几何假象。
   - 几何来源：`data/cross_benzoin/cross_round*/` 里 AL 选中对的产物几何，或
     `cross_round10` 的归档；醛几何从 `homo_v6` 库或 r89/r10 regen 目录。
   - 做法：把 `cross_benzoin/cheap_baseline_pilot_worker.py` 加一个
     `--geom-from <archive>` 分支跳过 embed+opt，直接在归档几何上做三个 SP。
2. **确认后全量 B97-3c 基线重算**：35k 已标注 pair × 3 物种，B97-3c SP ~5-15 min/
   分子，genoa/rome sharded array 一周内完成。产出 `dG_b973c_kcal` 列并进
   `assemble_cross_training_table_*` / champion 特征管线。
3. **用 B97-3c 基线重训 Δ-model**（`dG_pred = dG_b973c + ML 修正`），骨架不相交
   holdout 对比 champion 2.215。掉到 ~1.5 或更低 = 项目级突破。
- **风险**：B97-3c 残差的 1.11 std 里可能有一部分是标签构象噪声（pilot 里被同几何
  设计消掉了），生产中会重现；真实地板 ≈ max(1.1, 构象噪声 ~2.3)。即便如此从 2.215
  降到 ~2.3 以下仍是净胜。

#### 建议 2：把 homo 和 cross 合并成一个统一 Δ-learning 模型

**依据**：B 分解证明对等切分口径+规模下 **cross ≈ homo**（甚至 cross 略好），没有
"任务本质不同"的理由分开维护。C 显示当前 ~1:1 规模下 naive_merge 有 ~0.11 kcal
稳定增益（单-XGB + ensemble 两个模型类都验证）。当前两条线（homo `ENSEMBLE72` +
cross r1-10 blend）各自训练/部署/共享同一醛库——重复维护，且 09-06 跨子项目 bug
（`predict-dg-g-gxtb-regression-fixed`）正是这种耦合没管好的代价。

**执行步骤**：
1. 给 30k `homo_unify_v1` 做完整 **260-schema featurize**（mordred + assemble，把
   homo 当一个"round"塞进现有管线，~1-2 天）。数据在
   `data/cross_benzoin/homo_unify/`（`homo_unify_v1_{dft,products,scaffold_split_lookup}.csv`）。
2. homo 降权到 ~1:1（避免全量 6:1 稀释），并入 cross r1-10 训练集，重训 ensemble +
   GNN。骨架不相交 holdout 对比：C 的 −0.1 能否传导到 260-feat + GNN champion。
3. 若传导 → 再投 **GNN homo-pretrain → cross-finetune**（当前 champion GNN 是纯
   cross，`train_cross_gnn_scaffold_disjoint.py` 有 `--init-from-homo` 但 purge 后
   没重建过 homo state-dict；rounds 5-9 pre-purge 时这条路有效）。
4. 统一后 `predict_dg.py` 一个入口同时服务 A+A 和 A+B，`is_homo` 作为特征。
- **风险**：增益可能在强 champion 上缩水（C 的 caveat）；若 260-feat 后 naive_merge
  变 null 就止于此——至少澄清"开放问题1"并统一了代码路径（工程收益仍在）。

#### 建议 3：停 ~2k 规模 AL 轮，AL 预算转向还有空间的三处

**依据**：round10 AL 双层消融——诊断层阳性（挑的对确实差 28%），**修复层阴性
（ΔMAE −0.033）**。继续 round11/12 的 ~2k 轮是在明确证据下浪费 DFT 算力。

**三个替代方向（按优先级）**：
1. **定向攻 `baseline_risk` 子结构**：A 已识别超价 P / 磺酰基 / Se / N-氧化物 /
   硝基 = g-xTB 基线失败区（flagged MAE 2.86 vs 2.10）。定向富集这批子结构做 DFT
   ——这是"覆盖难"而非"噪声难"，AL 可能真能帮上。和建议 1 天然配合（这批分子正是
   B97-3c 相对 g-xTB 改善最大处，D 里 hetero hard-tail std 5.1 → 1.2）。
2. **测规模阈值**：round10 的 null 可能只说明"1,370 行 = +6.6% 太边际"。做一次
   **10k+ 大批次** AL，看是否存在规模阈值让增益显现（学习曲线在 frac 0.75 还没平台）。
3. **转 scaffold 覆盖扩展**：承认 in-domain MAE 到顶，AL 改成 query-by-committee 挑
   全新骨架 + 库扩张。改变的是适用域（对新化学空间的鲁棒性）而非 in-domain 精度
   ——骨架泄漏勘误后一直没系统做，且和 Goal 3 部署直接相关（用户最需要"这个新对
   在不在适用域内"）。
- **风险**：方向 1/2 仍可能 null（标签地板硬）；方向 3 几乎必然有价值。

**建议的推进顺序**：先起建议 1 的生产几何复核（1 天出结果，决定整个项目值不值得
换基线体系）；同时并行建议 2 的 homo 260-featurize（长跑，不阻塞）；建议 3 方向 1
可以和建议 1 的全量重算合并批次。

---

## 2. 在跑 / 相关的 SLURM 作业（09-07 傍晚）

| jobid | 名 | 状态 | 完成后 |
|---|---|---|---|
| `26397167` | `clean_orphans` | ✅ 健康在跑，`fat_rome`，janitor 持续服务 | 继续跑，不用管 |

**只有这一个本项目作业。** 第二个 `clean_orphans`（`26373560`）已到 24h 墙钟退出
——1 个足够（HANDOFF 惯例），guardian 只在探测到 0 个时才重投。

**不是我的、别碰**：所有 `nhc-*` / `*_cron`（NHC 窗口）、`mace-delta-train`、
`orcasp_INT2_*` / `int2-*`。

**集群健康**：09-07 傍晚 `sinfo -s` 显示所有分区（含白天 drain 过的 genoa /
fat_genoa / gpu_h100）已恢复 up。提交新作业前仍习惯性 `sinfo -s` 查一遍。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

本会话结束时有一个 guardian（`br1t5xw9m`，persistent，30 分钟一轮）——随会话失效。
新会话重建（Monitor 工具，内联 bash，30min）：
- **`clean_orphans` 存活**：`squeue -u schen3 -h -n clean_orphans | wc -l`，连续 2 次
  探测到 0 就 `sbatch --partition=<健康分区> pipeline/bde/clean_orphan_nodespec_workdirs.sh`
  重投（分区先 `sinfo -h -p fat_rome -o "%a"` 判健康，不健康转 `rome`）。
- **scratch1 inode 阈值**：`myquota` 里的 inode %，跨 75/90/98% 才告警。现 20.4%。
- 若起了建议 1/2/3 的长跑作业，加一条对应 array 的完成/里程碑监控（节流：只在跨
  200 任务里程碑 / 失败数变化 / 完全跑完时 report，轮询 600s——教训见早先 RUN_LOG）。

---

## 4. 未提交 / 未 push 的 git

**干净——本会话结束时全部已提交并推送。** HEAD = `f4ebb1d`。
```
git log origin/agent/recovery-20260902..HEAD --oneline | wc -l   # 应为 0
```
`git status --short` 剩的都是长期存在的、之前会话留下的：`FILE_MAP.md` 一处未提交
改动（不是本会话做的，别乱动）、round9 模型文件的 `M`（其他会话产出）、一堆探针/
中间产物未跟踪目录（含 `cheap_baseline_pilot/chunks/`——D 的碎 csv，无需提交）。

---

## 5. 关键决策 & 不要重新讨论的事

- **champion = r1-10**（blend MAE 2.215）。不变。
- **几何方法偏差 = 无偏差**，标签质量调查结案（`dg_geom_method` 09-04）。不要再提
  定向 g-xTB/r2SCAN 重标签战役。
- **round10 AL（2k 规模）= null，多构象重标签 = null，标签噪声地板 ≈ 2.9**（三条
  独立证据链）。**建议 3 就是这条结论的推论：停 ~2k AL 轮。**
- **3D-GNN（cross）无清晰增益；Phase-3 3D BDE 模型不跑**（查证后是 dG 目标脚本 +
  架构族已验证 null）。
- **homo 不比 cross 容易**（对等切分口径下，B 分解）。两条线可以合并（建议 2）。
- **BDE champion = B6 5-seed deep ensemble，醛 1.851 / 产物 2.826**（09-06 全量
  220k 重训，旧的 42k 库数字 1.579/3.060 作废）。
- **B97-3c 基线杠杆是新的、未验证的机会**（建议 1）——D pilot 是几何未复核的
  proxy，别当已确认的结论引用，先做生产几何复核。

---

## 6. 已知陷阱

- **共享库文件跨子项目耦合**：`data/cross_benzoin/homo_v6/aldehydes_all.csv` /
  `products_all.csv` 被 BDE 和 cross-dG 同时读写。**任何改这两个文件 schema 的工作
  （建议 1 会加 `dG_b973c_kcal` 列、建议 2 会动 featurize），改完必跑
  `predict_dg.py` 的 20-pair smoke test**（从 `cross_round10_dft_products.csv` 抽 20
  行，`/home/schen3/venv/nequip/bin/python cross_benzoin/predict_dg.py --products-csv
  <20行> --out /tmp/...`）。完整记录：memory `predict-dg-g-gxtb-regression-fixed`。
- **thermo_orca 循环 import**：任何新 worker 里 `import thermo_orca` 之前必须先
  `import conf_funnel_v3`（D 的 worker 第一次提交因此秒挂）。
- **临时文件写共享 GPFS 路径**，不能用提交节点本地 `/tmp`（计算节点看不到）。
- **`scancel` 会被 auto-mode 分类器拦截**；`scontrol update`（throttle）不会。需要
  kill 作业时先说明意图让用户决定。
- **提交新 SLURM 作业前 `sinfo -s`**——09-07 白天 genoa 整个 drain 过（傍晚已恢复）。
- **B/C 的本地 XGB+MLP 训练在登录节点上慢**（49k 行 ensemble ~45min，登录节点竞争）
  ——长跑走 SLURM，别在登录节点 nohup 大训练。
- 更早的陷阱（g-xTB 路径硬编码、float-id join bug、ORCA SCF 关键字、GPFS git 卡顿、
  scratch1 inode 孤儿等）见 `PROJECT_SUMMARY_20260904.md` 和更早 HANDOFF/RUN_LOG。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **`PROJECT_SUMMARY_20260907.md`（中）/ `_EN.md`（英）** | **当前汇报底稿**，围绕 AL 主线 + ΔG 预测，图文并茂，含 §6 A/B/C/D 复盘。中英保持同步 |
| `PROJECT_SUMMARY_20260904.md` | 上一版，历史快照（BDE 时间线更详细），已被上面取代 |
| `RUN_LOG_20260903.md` | 逐步日志（jobid / 决策 / bug，living doc） |
| **本文件 `HANDOFF_20260907.md`** | 会话交接；§1.2 = 三条建议（下个会话主线） |
| `data/analysis/homo_cross_gap/FINDING.md` | B（差异分解）+ C（联合 tabular）+ D（便宜基线 pilot）的完整数字 |
| `cross_benzoin/CHAMPION.md` | champion 加载方式、数字、lineage、输出列（含 A 新增的 conformal / baseline_risk 列） |
| `ALDEHYDE_LIBRARY_AND_DG_WORKFLOW_20260907.md`（中）/ `_EN.md`（英） | 醛库构建史 + dG 工作流 + 描述符工程技术参考 |
| `pipeline/analysis/make_summary_figures.py` → `docs/figures/*.png` | 汇报文档配图（重跑：直接跑脚本） |
| `pipeline/bde/STATUS.md` | BDE 子课题权威入口 |
| `cross_benzoin/{cheap_baseline_pilot_worker,merge_cheap_baseline_pilot}.py` + `pipeline/slurm/submit_cheap_baseline_pilot.sh` | 建议 1 的 pilot 脚本（生产几何复核在此基础上改 `--geom-from` 分支） |
| `cross_benzoin/{build_predict_dg_calibration,homo_cross_joint_tabular}.py` + `pipeline/analysis/homo_cross_gap_decomposition.py` | A / C / B 的脚本 |
| memory `~/.claude/.../memory/*.md` | 见 `MEMORY.md`；本会话新增 `gap-closure-20260907`、`user-prefers-chinese` |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit -F <msg 文件>`；
   `git push`（09-07 起有远程）。git 卡就在第 4 节精确写清「已 staged 什么、msg 在哪、
   接手第一件事」。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根，同一天多次交接覆盖当天这份），用本文件
   这套小节结构：`0 一句话` / `1 立即行动`（带确切命令）/ `2 在跑的 SLURM 作业`
   （表格 + 标注别碰的）/ `3 会话内监控`（**强调随会话失效，给重建命令**）/
   `4 未提交 git` / `5 关键决策` / `6 已知陷阱` / `7 文档索引` / `8 本节（复制过去）`。
3. **`RUN_LOG_<date>.md` 收尾**：最后一行进度快照 + 任何半截状态。
4. **更新 memory**：`MEMORY.md` 里指向新 HANDOFF 的一行；非显然的新事实写成 memory 文件。
5. **产出 resume prompt**（贴给新会话），模板见下。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（全项目：cross-benzoin dG 主线 + BDE 自动化，单会话统一驱动）。中文回答。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_20260907.md，第 0-1 节是立即行动。
2. 本会话主线 = 推进第 1.2 节的三条建议：
   - 建议 1（最高优先）：B97-3c 基线杠杆——先做生产 funnel_v3 几何上的 30 对复核，
     确认 D pilot 的 std 比值 0.26 不是几何假象；确认后规划全量 B97-3c 基线重算 + Δ-model 重训。
   - 建议 2（并行长跑）：homo_unify 30k 做完整 260-schema featurize，homo 降权 1:1 并入 champion 重训，看 C 的 +0.11 能否传导。
   - 建议 3（可合批）：停 ~2k 规模 AL 轮；方向 1（定向攻 baseline_risk 杂原子子结构）可和建议 1 的重算合并批次。
3. 会话内监控全失效——按第 3 节重建（clean_orphans 存活 + inode + 新起作业的完成监控）。
4. 提交任何新 SLURM 作业前先 sinfo -s 查分区健康。
5. 并行的 NHC 窗口(schen3-bc/schen3-7a)、mace-delta-train、orcasp_INT2_* 不要碰。
6. 改 aldehydes_all.csv / products_all.csv schema 后必跑 predict_dg.py 的 20-pair smoke test。
7. 进展记进 RUN_LOG_20260903.md（或当天新建）；会话结束按第 8 节套路交接，记得 git push。
```
