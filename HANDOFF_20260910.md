# 交接文档 — 2026-09-10

> 写给下一个接手的会话。**先读第 0-1 节。** 结构固定，见第 8 节。
>
> **本会话（09-10）**：(1) 09-09 中断会话找回 + 产物 mordred 恢复校验；(2) 用户新要求
> = homo **从零重做全库** + 补算丢失的 DFT 标签 + **单模 XGB + 单个 GNN**（不用 champion
> blend）；(3) 效率追问 → 从「自洽 regen ~3 周」改「**SP-only on 归档几何 ~2 天**」并**已启动**。
>
> **自主推进权限（惯例，每次交接都带）**：接手会话对本文件 §1 + §5 已确立的计划有
> **自主推进权** —— 已知的下一步直接做（起/并/续作业、按 DRAIN_RUNBOOK 执行、写 merge/
> assemble、跑单 XGB + 单 GNN、写进度、git push），**无需逐步征询**。只有遇到**真正的新
> 分叉**或**大额不可逆计算**（>~50k CPU-h 的全新战役、删数据、对外发布）才停下来问。
> NHC / kinetics / mace / orcasp / qm-benzoin / sourceB-kinetics / recalA-wb97mv 一律不碰。

---

## 0. 一句话

**champion 仍是 cross r1-10（blend MAE 2.215，未变）。** 两个战役并行在跑：

1. **cross Tier B**（自洽 B97-3c relabel，35,528 对）：**~24.2k/35.5k（68%），0 失败**，
   3 臂满载，**ETA drain ~09-11**。drain 后按 `DRAIN_RUNBOOK.md` step 1-6 做 cross
   champion+GNN 重训（`BASELINE_COL=dG_b973c_kcal`，schema v2=257，GNN 4-seed/w0.75）。
2. **homo SP-only relabel**（本会话新起）：在**归档 GFN2-opt 几何**上只做 DFT SP
   （r2SCAN-3c 标签 + B97-3c 基线），复用存好的 xTB 热校正 —— 不重生成几何、不算 Hessian。
   184,052 对，2 臂（`26540500` fat_genoa + `26540501` genoa），**~96/9203 shard 已落
   （~2h），ETA ~2 天**。drain 后 → `merge_homo_sp.py` → assembler `--full-library` →
   **单模 XGBoost Δ-model + 单个 attentive GNN**（`data/cross_benzoin/homo_standalone/`）。

**备份彻查确认**：homo 全库 DFT 标签物理丢失，git / scratch 老+新 repo / 87 个 home
tarball 全无。但**几何 + xTB 热力学全幸存** → 所以能走 SP-only 快线。

全部代码/文档已 push（`agent/recovery-20260902`，HEAD `0420a1f`）。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -u schen3 -o "%.13i %.16j %.10P %.2t %.10M %R" | grep -vE "nhc-|_cron|int2|orcasp|qm-benzoin|mace|kinetics|wb97mv"
/usr/hpc/bin/myquota | grep -i inode
git log --oneline -6 ; git status --short | grep -vE "cross_round9|FILE_MAP"
sinfo -s
# Tier B 进度:
awk -F, 'FNR>1{t++}FNR>1&&$6==""{e++}END{print t+0" pairs, "e+0" fail"}' \
  data/cross_benzoin/rec1_b973c_tierB/shards/shard_*.csv
ls data/cross_benzoin/rec1_b973c_tierB/shards/*.done | wc -l   # /4441
# homo SP 进度:
ls data/cross_benzoin/homo_standalone/relabel_sp/shards/*.done | wc -l   # /9203
awk -F, 'FNR>1{t++}FNR>1&&$5==""{e++}END{print t+0" pairs, "e+0" fail"}' \
  data/cross_benzoin/homo_standalone/relabel_sp/shards/shard_*.csv
```

### 1.2 cross Tier B drain 后（monitor `bhh9fodeh` 报 "ALL 3 ARMS DRAINED"）
按 `data/cross_benzoin/rec1_b973c_tierB/DRAIN_RUNBOOK.md` **step 1-6**：
merge → patch 表（schema v2=257）→ champion 重训（`CB_BASELINE_COL=dG_b973c_kcal`）→
GNN 4-seed/w0.75 → blend eval → 若 holdout MAE ~1.0-1.5 则重写 CHAMPION.md/PROJECT_SUMMARY。
**DRAIN_RUNBOOK step 7 = 起 homo SP 战役的命令 —— 本会话已提前起了，忽略 step 7。**

### 1.3 homo SP relabel drain 后（monitor `b6mldvcwi` 报 "both arms drained"）
Phase 2/3，全文 `data/cross_benzoin/homo_standalone/CAMPAIGN_PLAN.md`：
```
PY=/home/schen3/venv/nhc-workflow/bin/python
HS=data/cross_benzoin/homo_standalone
# 1. merge + QC
$PY cross_benzoin/merge_homo_sp.py
#   -> $HS/relabel_sp/homo_sp_{merged,labels,summary}.csv/.json
#   读 _summary.json: qc_repro_r2scan_vs_stored_30k.std 应 < ~2.9 (噪声地板), mean ~0
#   (smoke: +0.06 / +8e-6 / +2.9)。verdict GREEN = SP 干净无偏移 -> 继续。
#   真失败(dG_r2scan 空)的 shard 段: sbatch --partition=<idle> --array=<段>%200 \
#     cross_benzoin/slurm/submit_homo_sp.sh
# 2. 组装全库表 (--full-library 模式待加, 见 §5 + CAMPAIGN_PLAN Phase 2)
$PY cross_benzoin/assemble_homo_standalone_table.py --full-library \
    --labels $HS/relabel_sp/homo_sp_labels.csv \
    --out $HS/homo_full_train_table_slim260.parquet
#   列已核: products_all.csv 有 37/38 产物特征; bde <- products_bdfe_gxtb_descriptors.csv;
#   split <- products_scaffold_split.csv; target=dG_orca_kcal(=r2scan), baseline=dG_b973c_kcal
# 3. 单模 XGBoost Δ-model (复用 train_scaffold_disjoint.py 的 single-XGB 头)
CB_BASELINE_COL=dG_b973c_kcal $PY cross_benzoin/train_scaffold_disjoint.py \
    --table $HS/homo_full_train_table_slim260.parquet --outdir $HS/tabular_full --seed 42
#   报 scaffold_disjoint_holdout_xgb.MAE/R2 (ensemble 那个是附带参考, 不是要的)
# 4. 单个 attentive GNN (seed 0, 无集成)
sbatch --partition=<idle fat_rome|rome> cross_benzoin/slurm/submit_homo_standalone_gnn.sh
#   先把 submit_homo_standalone_gnn.sh 里的 TABLE 指到 homo_full_train_table_slim260.parquet,
#   OUT 指到 $HS/gnn_full_seed0, 环境加 CB_BASELINE_COL=dG_b973c_kcal; 报 gnn_only_mae
# 5. 写 $HS/README.md 的 Results: 单 XGB / 单 GNN 的 holdout MAE, 对比 cross champion 2.215
```

### 1.4 会话内监控全失效 —— 按 §3 重建

---

## 2. 在跑 / 相关的 SLURM 作业（09-10）

| jobid | 名 | 状态 | 完成后 |
|---|---|---|---|
| `26467946` | tierB_rm | Tier B rome 0-1799，128R | → §1.2 DRAIN_RUNBOOK |
| `26467948` | tierB_fg | Tier B fat_genoa 3120-4440 | → §1.2 |
| `26468648` | tierB_fg2 | Tier B fat_genoa 1800-3119 | → §1.2 |
| `26540500` | homo_sp | homo SP relabel，fat_genoa 0-4600%150 | → §1.3 |
| `26540501` | homo_sp | homo SP relabel，genoa 4601-9202%120 | → §1.3 |
| `26464656` | clean_orphans | janitor，fat_rome，健康 | guardian 会重提 |

**已结束（本会话）**：`26485904` 产物 mordred 恢复 ✅ / `26537710/13` 30k 版模型
（取消，用户改全库）/ homo relabel smoke `26538271`（自洽 regen，−5.5 偏移，弃用）/
homo SP smoke `26539727`(撞车挂→修) `26540057`(pass) `26541807`(增量写 pass)。

**不是我的、别碰**：`nhc-*` / `*_cron` / `mace-*` / `orcasp_*` / `qm-benzoin-*` /
`sourceB-kinetics` / `recalA-wb97mv`。

**集群（09-10）**：rome ~40-90 idle；fat_genoa/genoa 被 Tier B + homo SP + 用户 NHC
占；fat_rome 满。提交前 `sinfo -s`。genoa 只短借；fat 贵 50%。QOS `MaxJobsPU=128`
（rome/genoa）—— 我的 rome 被 Tier B rome 臂占满，homo SP 走的 fat_genoa/genoa。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

- **guardian `btv3jsucr`**（1800s persistent）—— clean_orphans 连续 2×0 → 提示重提；
  scratch1 inode 跨 75/90/98% 告警。脚本 `scratchpad/session_guardian.sh`。
- **Tier B campaign `bhh9fodeh`**（900s）—— 3 臂（`26467946 26467948 26468648`）：每 +2k
  pair 报 / 真失败上升 / 3 臂全 drain → 触发 §1.2。脚本 `scratchpad/tierB_campaign_monitor.sh`。
- **homo SP campaign `b6mldvcwi`**（900s）—— 2 臂（`26540500 26540501`）：每 +3k pair 报 /
  真失败(col5 空)上升 / 两臂 drain → 触发 §1.3。脚本 `scratchpad/homo_sp_campaign_monitor.sh`。

新会话重建（Monitor 工具，内联 bash 或重写 scratchpad 脚本）：
```bash
# guardian: n=$(squeue -u schen3 -h -n clean_orphans|wc -l) 连续2次0→resubmit;
#           pct=inode% 跨 75/90/98
# Tier B:   D=data/cross_benzoin/rec1_b973c_tierB/shards; ndone=ls $D/*.done|wc -l;
#           inq=for j in 26467946 26467948 26468648; squeue -h -j $j; 0→§1.2
# homo SP:  H=data/cross_benzoin/homo_standalone/relabel_sp/shards; ndone=ls $H/*.done|wc -l;
#           nfail=awk -F, 'FNR>1&&$5==""{c++}END{print c+0}' $H/shard_*.csv;
#           inq=for j in 26540500 26540501; squeue -h -j $j; 0→§1.3
```

---

## 4. 未提交 / 未 push 的 git

**干净 —— 本会话所有产出已提交并推送**（HEAD `0420a1f`）。`git status` 剩的都是长期
非本会话项：`FILE_MAP.md`、round9 模型文件 `M`（其他会话）、一堆 gitignore 的 DFT/
中间产物未跟踪目录（含 `homo_standalone/relabel_sp/shards/`、`rec1_b973c_tierB/shards/`）。

---

## 5. 关键决策 & 不要重新讨论的事

- **champion = cross r1-10**（blend MAE 2.215）。不变，直到 Tier B 重训出更好的数。
- **Tier B 已起（用户 go），不重议。** drain 后 = DRAIN_RUNBOOK step 1-6。
- **homo 从零 = 用户 09-10 定型**：全库（不是 30k 捷径、不是往旧标签加列）；缺的 DFT
  标签**补算**；模型 = **单模 XGBoost + 单个 GNN，各自独立报**（不用 champion blend /
  seed 集成 / 迭代调参）。放 `data/cross_benzoin/homo_standalone/`。
- **homo 补算 = SP-only 快线，不是自洽 regen**（用户："三周太久"）。在 09-02 BDE
  featurize 的归档 GFN2-opt 几何上只做 DFT SP，复用 CSV 里存好的 xTB 热校正。
  smoke 验证：新 r2SCAN-3c 复现旧 30k 标签 repro +0.06/+8e-6/+2.9（**无系统偏移**；
  自洽 regen 那条有 −5.5 → **弃用**，脚本保留在 repo）。
- **homo 补算标签基线 = B97-3c**（`dG_b973c_kcal`）。**新标签集不和旧 30k 混用**
  （几何 vintage 不同；绝对偏移对 from-scratch 模型无害）。
- **homo SP 战役已在本会话启动**（26540500/26540501）—— DRAIN_RUNBOOK step 7 忽略。
- 备份彻查完毕：homo 全库 DFT 标签**无任何备份**（git/scratch/home 全查）。**不要再花
  时间找备份。**
- **产物 mordred 已恢复**（218,916 行，09-09 job 26485904，从 2026-07 home 备份）。
- geom 方法偏差=无偏差；round10 AL=null；多构象重标=null；3D-GNN 无增益；homo 不比
  cross 容易。均不重议。
- **BDE champion = B6 5-seed deep ensemble，醛 1.851 / 产物 2.826**。
- **交接是惯例**：每次会话结束/被打断都写 HANDOFF + resume prompt（带自主推进权限）。

---

## 6. 已知陷阱

- **共享库文件跨子项目耦合**：`homo_v6/aldehydes_all.csv` / `products_all.csv` 被 BDE
  和 cross-dG 同读写。改 schema 必跑 `predict_dg.py` 20-pair smoke（memory
  `predict-dg-g-gxtb-regression-fixed`）。
- **`calc_orca_sp` 在 xyz 旁建 `orca_sp/`**：并发同一几何多个 SP 会撞目录 → 全挂。
  `homo_sp_from_geom_worker._sp` 已用每-SP 独立 tempdir（照 `dft_sp_from_geom.py`）。
- **homo SP worker 增量写**：`as_completed`，每对算完立即 append+fsync（原来攒 20 对
  一次写 → 撞墙丢整 chunk）。断点续跑按 `id`。
- **恢复 CSV 的 "2.0" 式 float id**：string-merge 到 "2"-id 库会 join 到 ~0 行。用
  `qc.norm_id`（commit 826c0e9）。manifest builder 用 `pd.to_numeric`。
- **thermo_orca ⟲ conf_funnel 循环 import**：worker 里先 `import conf_funnel_v3` 再
  `import thermo_orca`。
- **ORCA**：`ORCA_SCF=default`（TightSCF 会 hang / 超时）；`-3c` 方法不传 basis；PATH 含
  `/home/schen3/orca`。r2SCAN-3c SP ~6.7 min（60 原子，单核）。
- **多分区 `--partition=a,b` 对 shared job 不允许**；一分区一个 sbatch。
- **临时文件用 `/scratch-local` / `$TMPDIR`**，不用登录节点 `/tmp`。
- **本地大 concat / read_csv 别在登录节点**（5.3GB mordred CSV、184k 行 os.path.exists
  都会超时）；走 SLURM 或后台 + 耐心。
- **FS 在两战役并行时很慢**：`ls`/`awk` over shards、`git status` 都可能 >120s。
  后台跑 + 等，别急着重试。
- **fat 节点贵 50%**（`TRESBillingWeights cpu=1.5`）。**提交新作业前 `sinfo -s`。**
- 更早陷阱见 `HANDOFF_20260908.md` §6 / `PROJECT_SUMMARY_20260907.md`。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **本文件 `HANDOFF_20260910.md`** | §0-1 = 两战役状态 + drain 后步骤 |
| `HANDOFF_20260908.md` | 上一份；§1.5 = homo relabel（本文件 §5 已更新定型）；Tier B drain 细节 |
| `data/cross_benzoin/rec1_b973c_tierB/DRAIN_RUNBOOK.md` | Tier B drain step 1-6（cross retrain）；step 7 已过时（homo SP 已起）|
| `data/cross_benzoin/homo_standalone/CAMPAIGN_PLAN.md` | homo 从零 3 阶段全文（SP-only route + 弃用的自洽 route）|
| `data/cross_benzoin/homo_standalone/README.md` | homo standalone 目标 + 数据盘点 + Results（待填）|
| `data/cross_benzoin/homo_v6/LIBRARY_MANIFEST.md` | homo 库文件行数/覆盖率/id 规范（含 09-09 恢复的产物 mordred）|
| `cross_benzoin/build_homo_sp_manifest.py` | homo SP manifest（184,052 行）|
| `cross_benzoin/homo_sp_from_geom_worker.py` | homo SP worker（2 fix：orca_sp tempdir + 增量写）|
| `cross_benzoin/slurm/submit_homo_sp.sh` | homo SP array（CHUNK 20 → 9,203，--array/--partition 覆盖）|
| `cross_benzoin/merge_homo_sp.py` | Phase 2 step 1：shards → labels + repro verdict |
| `RUN_LOG_20260903.md` | 逐步日志（living doc，09-10 段最新）|
| `cross_benzoin/CHAMPION.md` | cross champion 加载/数字/lineage |
| memory `~/.claude/.../memory/*.md` | 见 `MEMORY.md` |

---

## 8. 交接套路（本节固定，下次照做）

**会话结束 / 快被打断时，按顺序：**

1. **提交或明确未提交**：`rm -f .git/index.lock; git add <改动>; git commit; git push`。
   git 卡就在第 4 节写清「已 staged 什么、msg、接手第一件事」。
2. **写 `HANDOFF_<YYYYMMDD>.md`**（repo 根，同一天多次交接**覆盖**当天这份），用本文件
   小节结构：`0 一句话` / `1 立即行动`（确切命令）/ `2 SLURM 作业`（表 + 别碰的）/
   `3 会话内监控`（强调随会话失效 + 重建命令）/ `4 未提交 git` / `5 关键决策` /
   `6 陷阱` / `7 文档索引` / `8 本节`。**顶部写「自主推进权限」段。**
3. **`RUN_LOG_<date>.md` 收尾**：最后一行进度快照。
4. **更新 memory**：`MEMORY.md` 指向新 HANDOFF 的一行；非显然新事实写 memory 文件。
5. **产出 resume prompt**（贴给新会话），模板见下 —— **必须含自主推进权限条款**。

**Resume prompt 模板**（用户新开对话时贴，改日期）：
```
继续 benzoin-dg 项目（全项目：cross-benzoin dG 主线 + BDE 自动化，单会话统一驱动）。中文回答，
关键结论中英双语。

0. 你对本 handoff §1 + §5 已确立的计划有【自主推进权限】：已知的下一步直接做（起/并/续
   SLURM 作业、按 DRAIN_RUNBOOK 执行、写 merge/assemble、跑单 XGB + 单 GNN、写 RUN_LOG、
   git push），无需逐步征询。只有遇到真正的新分叉、或 >~50k CPU-h 的新战役 / 删数据 /
   对外发布 才停下来问。NHC(schen3-bc/7a)、nhc-*、mace-*、orcasp_*、qm-benzoin-*、
   sourceB-kinetics、recalA-wb97mv 一律不碰。

1. 读 /gpfs/scratch1/shared/schen3/benzoin-dg-restored/HANDOFF_20260910.md，§0-1 是立即行动。
2. 两个战役并行：
   (a) cross Tier B（自洽 B97-3c relabel）—— campaign monitor drain 时按 DRAIN_RUNBOOK.md
       step 1-6 做 cross champion+GNN 重训（BASELINE_COL=dG_b973c_kcal，schema v2=257，
       GNN 4-seed/w0.75）。DRAIN_RUNBOOK step 7 忽略（homo SP 已起）。
   (b) homo SP-only relabel（26540500/26540501）—— drain 时按 §1.3：merge_homo_sp.py →
       assembler --full-library → 单模 XGBoost + 单个 attentive GNN（homo_standalone/）。
3. 会话内监控全失效 —— 按 §3 重建 guardian + Tier B campaign + homo SP campaign。
4. homo 从零方案已定型（§5）：全库、SP-only 补算、单 XGB + 单 GNN、不用 champion blend、
   不和旧 30k 混用。不要再花时间找 homo 标签备份（已彻查，无）。
5. 提交新 SLURM 前 sinfo -s。genoa 只短借；fat 贵 50%；QOS MaxJobsPU=128 不放宽。
6. 改 aldehydes_all.csv / products_all.csv schema 后必跑 predict_dg.py 20-pair smoke。
7. 进展记进 RUN_LOG_20260903.md；会话结束按 §8 套路交接（惯例），resume prompt 带自主
   推进权限，记得 git push。
```
