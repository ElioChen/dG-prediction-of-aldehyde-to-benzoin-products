# 交接文档 — 2026-09-20

> 写给下一个接手的会话。**先读第 0-1 节。** 结构照 `HANDOFF_20260916.md` 的固定套路，见第 8 节。
> **上一份交接是 09-16 的。用户开场问「周四中午后是否主动推进了」——答案是没有：最后一条
> commit 是周四(09-17) 14:39 的 `116ceac`，之后 3 天零 commit、零新提交的 SLURM job。
> 但排查发现两条线其实早就跑完了算力（周四晚 / 周五凌晨），只是没人回来收尾——本次会话
> 把这两条线都收尾了，没有新开算力战役，只是把已完成的结果接上后续步骤。**

**本会话（09-20）做了什么**：(1) 发现 BDE-CRG 消融的 seed1-3 验证 job（`26843134-139`）
早在 09-18 05:45 就 COMPLETED，合并 seed0 得到最终 4-seed 结论并写入
`pipeline/bde/STATUS.md`§十；(2) 发现 homo SP relabel 两条 track 周四晚就已 100%
（archived 8972/8972，regen 771/771），但 merge 脚本没重跑——重跑后 QC GREEN，
随即提交了预先写好的 Phase 2/3 依赖链（`26947514`→`26947515`）；(3) 顺手排查了一次
`/gpfs/scratch1/shared/schen3/benzoin-dg`（不带 `-restored`）疑似 git 损坏的问题——
**结论：那是已知的 2026-07 purge 遗留空壳（见 memory
`benzoin-dg-repo-moved-and-data-loss`），跟本项目无关，不要在那个目录下工作**。

**自主推进权限（惯例，每次交接都带）**：接手会话对本文件 §1 + §5 已确立的计划有
**自主推进权**——已知的下一步直接做（起/并/续作业、按流程执行、写 merge/assemble、
跑训练、写进度、git push），**无需逐步征询**。只有遇到**真正的新分叉**或**大额不可逆
计算**（>~50k CPU-h 的全新战役、删数据、对外发布）才停下来问。
NHC / kinetics / mace / orcasp / qm-benzoin / sourceB-kinetics / recalA-wb97mv
一律不碰（不是本项目，账号共享）。

---

## 0. 一句话

**champion 仍是 cross r1-10-b973c**（holdout MAE 0.528，没变）。**BDE-CRG 标记消融
已经结束**：4-seed 确认无收益，不采用。**homo 从零重标的算力部分已经完全跑完**
（169,493/184,052=92.1%覆盖，QC GREEN），**Phase 2/3 训练已提交在跑**
（`26947514` 组装+单XGB，fat_genoa，6h；`26947515` 单GNN，fat_rome，24h，依赖
`26947514`）——**接手会话第一件事：查这两个 job 的结果**，写进 `LAB_JOURNAL.md`。

---

## 1. 立即行动

### 1.1 先对齐现状
```
cd /gpfs/scratch1/shared/schen3/benzoin-dg-restored
squeue -j 26947514,26947515
git log --oneline -5
git status --short
```

### 1.2 homo 全库单 XGB / 单 GNN 结果查看（第一优先）
```
cat data/cross_benzoin/homo_standalone/tabular_full/*/metadata.json 2>/dev/null
cat data/cross_benzoin/homo_standalone/gnn_attentive_full_seed0/result.json 2>/dev/null
tail -60 slurm_logs/homo_sa_full_26947514.out
tail -60 slurm_logs/homo_sa_gnn_full_26947515.out
```
**判断标准**：单独汇报 XGB 的 `scaffold_disjoint_holdout_xgb` MAE/R² 和 GNN 的
`gnn_only_mae`（`CAMPAIGN_PLAN.md`§Phase 3——**不做 blend，两个数字分开报**，这是
2026-09-10 用户的明确指示）。跟 cross champion blend MAE 0.528、g-xTB baseline
5.037、purge 前全库 homo 模型 MAE≈10 做对比语境。跑完后：
1. 写入 `LAB_JOURNAL.md`+`_ZH` 今天的条目（已开头，补完）。
2. 更新 `data/cross_benzoin/homo_standalone/CAMPAIGN_PLAN.md` 标注 Phase 2/3 完成。
3. `git add -f` 关键产出（table parquet 如果不超 100MB、模型 joblib/pt、result json），
   按 `pipeline/bde/STATUS.md`§七 / 本文件旧陷阱同样的备份纪律处理。
4. 如果两个模型的 MAE 明显优于/劣于旧的 pre-purge 数字，考虑要不要重跑
   Rec-2（homo+cross 统一实验，`data/analysis/homo_cross_gap/`）——旧结论(+0.11)是在
   旧 provisional homo 表上得到的，全库 homo 表落地后应该用新表重新验证一次，不能
   直接套用旧结论。

### 1.3 如果两个 job 还没跑完
6h/24h 墙钟，正常情况下会话内大概率等不到 GNN 跑完。**不需要同步等**——按 §3 起个
监控，转去做 §1.2 步骤4提到的 Rec-2 重跑准备工作，或处理下面 §4 的历史遗留问题。

---

## 2. 在跑 / 相关的 SLURM 作业（09-20 交接时）

| Job | 用途 | 分区 | 状态 |
|---|---|---|---|
| `26947514` | homo 全库：merge(已在本会话手动跑完)→assemble→单XGB | fat_genoa | R，6h 墙钟 |
| `26947515` | homo 全库：单 attentive GNN，`--dependency=afterok:26947514` | fat_rome | PD（等依赖） |
| `26673662` clean_orphans（如果还在） | inode 清理 janitor | fat_rome | 长期挂着，正常，不用管 |

**没有其他本项目在跑的 job**——`squeue -u schen3` 交接时只有一个无关的
`nhc_backup_cron`（PENDING，别的项目）。

**不要碰**：任何 `nhc-*`、`*_cron`、`orcasp_*`、`sys/dashboard/*` —— 账号共享，别的项目。

---

## 3. 会话内监控（**随会话结束失效，必须重建**）

本会话没有建立持久 Monitor。接手会话如果要盯 `26947514`/`26947515`：
```
squeue -j 26947514,26947515 -o "%.10i %.20j %.8T %.10M"
# 或直接看 slurm_logs/homo_sa_full_26947514.out / homo_sa_gnn_full_26947515.out 的更新
```

---

## 4. 未提交 / 未 push 的 git

**本会话结束时**：`LAB_JOURNAL.md`/`_ZH` + `pipeline/bde/STATUS.md` 已提交
（commit `e16c3b1`）。仍然保留、**本会话没有碰、也不建议碰**的历史遗留：

1. **6 个 `cross_round9` 模型文件的未解释改动**——09-16 交接就标注过"来源不明，先别
   动"，本会话再次确认状态未变（还是 `M`，未提交），继续保持原判：不确定就别动。
2. 一堆 `??` 未跟踪目录/文件（`cross_round10/gnn_crg_*`、`cheap_baseline_pilot/`、
   `confnoise_cross/` 等各战役中间产物）——同样是历史惯例，只 force-add 最终产出，
   中间 shard/per-seed 目录留在 scratch1 就行，不需要全部入库。
3. `data/analysis/homo_cross_gap/homo_cross_joint_tabular_v2_provisional.json`
   （09-16 的 Rec-2 pilot 产出，`provisional`，未跟踪）——本会话没有触碰这条线，
   见 §1.2 步骤4 关于要不要重跑的建议。

---

## 5. 关键决策 & 不要重新讨论的事

1. **BDE 产物侧不采用 CRG 目标键标记**——09-20 4-seed 最终结论，marked 均值反而更差
   且方差是 unmarked 的 5 倍，见 `STATUS.md`§十。不要重新提议"标记有没有用"，除非
   换了完全不同的标记方案。
2. **09-16 交接里 1-6 条决策依然有效**（`train_one` checkpoint-restore 修复既定、
   `torch.load weights_only=False` 是必须的、`products_all.csv` 不配 git-lfs、
   backup_benzoin_scratch_weekly.sh 不是本项目脚本不用管、BDE-CRG 旧消融已清理、
   09-06~09-16 历史结论 settled）——完整文本见 `HANDOFF_20260916.md`§5，不重复贴。
3. **homo 全库单 XGB / 单 GNN 必须独立汇报，不做 blend**——2026-09-10 用户明确指示，
   `CAMPAIGN_PLAN.md`§Phase 3 已经写死，不要图方便又做成 ensemble。

---

## 6. 已知陷阱（新增一条，其余见 09-16 §6）

1. **进度类脚本"跑完 shard 不等于跑完流程"**——`merge_homo_sp.py`/类似的合并脚本不会
   在 shard 100% 完成时自动触发，本次 3 天空窗期的根因就是"两条计算战役都跑完了，
   但没人手动触发下一步"。**以后任何长战役快接近 100% 时，优先检查有没有预先写好
   的下一步脚本/依赖链（本例是 09-17 早上已经写好的 `submit_homo_standalone_full.sh`
   + `_gnn_full.sh`），drain 后立刻手动跑 merge 确认 QC，别等下次会话才发现。**
2. **`/gpfs/scratch1/shared/schen3/benzoin-dg`（不带 `-restored`）是死目录**——
   `.git` 对象库是空的（2026-07 purge 遗留），这个环境的默认工作目录有时会指向这里
   （不是 `-restored`），**每次会话开始务必先 `cd` 到 `benzoin-dg-restored` 再干活**，
   不要被默认 cwd 或 IDE 的 git 状态面板误导成"这个项目没有 git 历史"。

---

## 7. 文档索引

| 文档 | 作用 |
|---|---|
| **本文件 `HANDOFF_20260920.md`** | §0-1 = 立即行动 |
| `HANDOFF_20260916.md` | 上一份；BDE-CRG bug 排查方法论、备份审计细节，仍然权威 |
| `LAB_JOURNAL.md` + `LAB_JOURNAL_ZH.md` | 09-20 条目 = 本会话完整叙事 |
| `pipeline/bde/STATUS.md` | BDE 子课题权威入口；§十 = 今天的 4-seed 最终结论 |
| `data/cross_benzoin/homo_standalone/CAMPAIGN_PLAN.md` | homo 从零 3 阶段全文，Phase 2/3 待接手会话跑完后标注完成 |
| memory `~/.claude/.../memory/*.md` | 见 `MEMORY.md`（会话结束后补一条指向本文件的索引） |

---

## 8. 交接套路（固定，照 09-16 §8 执行，不重复贴）
